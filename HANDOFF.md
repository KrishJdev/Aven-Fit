# HANDOFF.md

> **Canonical Handoff State** — Single persistent handoff file. Update in place at session end.
> **Last updated:** 2026-09-02

---

## Active Project & Stack

- **Project:** Aven Fit (Offline-first Gym & Nutrition Tracker for Android)
- **Mobile:** Flutter + Dart in `mobile/` (State: Riverpod 3.x, Local DB: SQLite via **Drift** — `drift_flutter` + `sqlite3_flutter_libs`, Navigation: **GoRouter 18**; Feature-First layout: `lib/core/` + `lib/features/<domain>/{domain,data,presentation}/`)
- **Backend:** Spring Boot 3.3 + Java 21 in `backend/` (Server DB: PostgreSQL 16)
- **Archive Policy:** `docs/archive/` and `archive/` are quarantined; never access unless explicitly instructed.

---

## Session Notes

- **2026-08-31 (session 1):** Slice 1 scaffolding started. Legacy `mobile/node_modules` (React Native era, untracked) removed. Flutter project created in `mobile/` (`--org com.avenfit`, project name `aven_fit` → package `com.avenfit.aven_fit`, Android-only). All locked dependencies configured in `pubspec.yaml` (Riverpod 2.x, GoRouter 14.8.x, Drift 2.24.x, Dio 5.8+, WorkManager 0.5.2, Freezed 2.5.x, google_fonts, lucide_icons; dev: build_runner, riverpod_generator, drift_dev, freezed, json_serializable, mocktail, patrol 3.x, flutter_lints 5). `flutter pub get` resolved cleanly (124 deps), `flutter analyze` passes with no issues. Next: Glassmorphism theme, Drift schema in `lib/core/`, GoRouter shell, Feature-First dirs.
- **2026-08-31 (session 2):** Slice 1 core foundation scaffolded — `core/theme` (Sharp Glassmorphism: OLED black, neon cyan, volt green, burnt orange, Inter + JetBrains Mono tabular numerals), `core/database` (Drift via `drift_flutter`, `shareAcrossIsolates`, `WorkoutSessions` base table, schema v1), `core/network` (Dio, baseline timeouts, interceptor hook), `core/router` (`StatefulShellRoute.indexedStack` with the 4 main tabs: Home · Workouts · Progress · Nutrition). Feature-First trees created under `lib/features/{workout,routine,nutrition,history,sync}/{data,domain,presentation}`. `main.dart` wires `ProviderScope` + `AppTheme` + `appRouter`; DB exposed via `appDatabaseProvider` (dispose-safe). Fonts bundled as SHA-256-pinned TTF assets (google_fonts never fetches at runtime — offline-first, deterministic tests). `build_runner` codegen OK (18 outputs); `flutter analyze` 0 issues; `flutter test` 1/1 passing.
  - **Forced deviation 1 (deps):** Dart 3.13/Flutter 3.47 breaks the originally locked codegen stack — `riverpod_generator` 2.x is pinned to `analyzer ^7`, which crashes on Dart 3.10+ dot-shorthand AST (`Missing implementation of visitDotShorthandPropertyAccess`). Full `pub upgrade --major-versions` applied: riverpod_generator 4.0.8, flutter_riverpod 3.4.2, riverpod_annotation 4.0.6, freezed 4.0.1 + freezed_annotation 3.1.0, go_router 18.0.0, drift/drift_dev 2.34.x, workmanager 0.10.9, google_fonts 8.2.1, patrol 4.9.0, build_runner 2.16.0, flutter_lints 6. **Requires user ratification to amend the AGENTS.md stack table (Riverpod 3.x / GoRouter 18 / WorkManager 0.10).**
  - **Forced deviation 2 (icons):** `lucide_icons` cannot compile on Flutter 3.47 — every version subclasses `IconData`, which the framework made `final`. Replaced with `lucide_icons_flutter` (maintained Lucide port, plain `IconData`, same icon names incl. `home`/`dumbbell`/`chartColumn`/`salad`). The spec's alternative `flutter_svg` was not used (heavier runtime + asset footprint).
  - **Note:** `build_runner` on the new build 4.x ignores `--delete-conflicting-outputs` (removed flag; conflicting outputs are handled automatically).
- **2026-08-31 (session 3):** **Ratification confirmed by user** — both forced deviations above are approved: (1) the `pub upgrade --major-versions` dependency set (Riverpod 3.x, GoRouter 18, Drift 2.34+, Freezed 4, WorkManager 0.10+, Patrol 4.9) for the Dart 3.13 / Flutter 3.47 toolchain, and (2) the `lucide_icons` → `lucide_icons_flutter` swap. `AGENTS.md` Key Tooling table updated with the ratified versions plus a ratification note; `lucide_icons_flutter` added to the locked tooling list. No code changes this session. Next: continue Slice 1 remainder / Slice 2 per milestone table.
- **2026-08-31 (session 4):** Slice 1 scaffold re-verified end to end (no code changes needed — session 2 output was intact and untracked). `dart run build_runner build --delete-conflicting-outputs` → 24 outputs (flag ignored on build 4.x, as noted in session 2); `flutter analyze` → 0 issues; `flutter test` → 1/1 passing. All 4 core modules (`core/theme`, `core/database`, `core/network`, `core/router`), 5 feature trees (`workout/routine/nutrition/history/sync` × `data/domain/presentation`), and `main.dart` wiring confirmed in place.
- **2026-08-31 (session 5):** Native bindings wired (Step 4 of Slice 1):
  - **Sentry:** `sentry_flutter 8.14.2` added (satisfies requested ^8.12.0). `SentryFlutter.init()` in `main.dart` with env-based DSN placeholder (`--dart-define=AVENFIT_SENTRY_DSN`, default placeholder → sending disabled), `AVENFIT_ENV` (default `development`), `tracesSampleRate 1.0`, `sendDefaultPii false`; `appRunner` wraps `runApp` so startup crashes are captured.
  - **WorkManager:** top-level `@pragma('vm:entry-point') void workmanagerDispatcher()` created in `lib/core/sync/workmanager_dispatcher.dart` with canonical task IDs (`WorkmanagerTasks.syncQueueDrain`). In 0.10.9 `executeTask` is **void** (registers handler, returns immediately — do NOT await) and the dispatcher must be registered via `await Workmanager().initialize(workmanagerDispatcher)` before task registration (Android resolves the callback handle from SharedPreferences). A network-constrained periodic task (`BackoffPolicy.exponential`, 15 min) is registered in `main()` as a no-op placeholder until Slice 5 wires the real queue drain.
  - **Patrol (Gradle):** `android/app/build.gradle.kts` (Kotlin DSL — project has NO Groovy `build.gradle`) configured per the official Patrol 4.9 example: `testInstrumentationRunner = "pl.leancode.patrol.PatrolJUnitRunner"`, `clearPackageData` runner arg, `execution = "ANDROIDX_TEST_ORCHESTRATOR"`, `androidTestImplementation("androidx.test:runner:1.5.2")` + `androidTestUtil("androidx.test:orchestrator:1.5.1")`. The bare androidx runner requested would NOT execute Patrol tests — `PatrolJUnitRunner` is required (it binds the Dart test registry).
  - **Mocktail smoke test:** `test/smoke_test.dart` (3 tests: stub/act/verify, argument predicates, harness sanity). All pass.
- **2026-08-31 (session 6):** Gradle test compilation issue investigated and resolved:
  - **Root Cause 1 (Cross-drive incremental cache crash):** The workspace lives on `E:\Projects\Aven Fit\` while Pub cache is on `C:\Users\Krish\AppData\Local\Pub\Cache\`. Kotlin incremental compilation attempts relative path calculation (`FilesKt__UtilsKt.relativeTo`) across drive letters on Windows, triggering `IllegalArgumentException: this and base files have different roots: C:\... and E:\...`. **Resolution:** Added `kotlin.incremental=false` to `mobile/android/gradle.properties`.
  - **Root Cause 2 (Kotlin languageVersion deprecation in plugins):** `sentry_flutter`'s Android build configured `kotlinOptions { languageVersion = "1.6" }`, which Kotlin 2.x rejects (`e: Language version 1.6 is no longer supported; use version 2.0 or greater instead`). **Resolution:** Configured `tasks.withType<org.jetbrains.kotlin.gradle.tasks.KotlinCompile>()` in `mobile/android/build.gradle.kts` `subprojects` block to enforce `languageVersion` and `apiVersion` to `KOTLIN_2_0`.
  - **Verification:** `.\gradlew.bat :app:compileDebugAndroidTestSources --no-daemon --console=plain` executes and succeeds cleanly (`BUILD SUCCESSFUL in 1m 45s`, all androidTest tasks compiled).
- **2026-08-31 (session 7):** Established Root Architecture Reference and Workout Reference Slice (Step 5 of Slice 1):
  - **Root Architecture Specification:** Created `ARCHITECTURE.md` establishing the Feature-First Clean Architecture blueprint, layer contracts (`domain/`, `data/`, `presentation/`), unidirectional data flow diagrams, stack laws, feature addition templates, and quality verification matrix.
  - **Workout Canonical Reference Slice (`mobile/lib/features/workout/`):**
    - **Domain Layer (`domain/`):** Implemented pure immutable Freezed domain models (`WorkoutSession`, `WorkoutSet`, `WorkoutStatus`, `SetType`).
    - **Data Layer (`data/`):** Defined Drift tables (`WorkoutSessions`, `WorkoutSets`), created `WorkoutDao` (`workout_local_source.dart`) with reactive Drift streams (`watchActiveSession()`, `watchSessionSets()`) and CRUD operations, implemented `WorkoutRepository` contract and `WorkoutRepositoryImpl` with `@riverpod` provider.
    - **Presentation Layer (`presentation/`):** Implemented `ActiveWorkoutState` immutable UI state, `ActiveWorkoutController` Riverpod 3.x `AsyncNotifier` (disallowing legacy ChangeNotifier/two-way binding; optimistic updates with SQLite write-through, rest timer management with epoch calculations), and `ActiveWorkoutScreen` declarative `ConsumerWidget` with Sharp Glassmorphism UI styling (`AppTheme.oledBlack`, `AppTheme.glassFill`, `AppTheme.glassBorder`, `AppTheme.neonCyan`, `AppTheme.voltGreen`, `AppTheme.burntOrange`, `AppTheme.num` tabular figures).
    - **Wiring & Routes:** Wired `/workout/active` route in `app_router.dart` and connected `HomePage` "START NEW SESSION" button.
    - **Build & Tests:** Configured `mobile/build.yaml` with `explicit_to_json: true`. Created `mobile/test/features/workout/workout_reference_slice_test.dart` asserting domain immutability/serialization, repository SQLite CRUD, and controller store state transitions.
    - **Verification:** `dart run build_runner build` (27 outputs), `flutter analyze` (0 issues), `flutter test` (12/12 passing).
- **2026-08-31 (session 8):** Implemented **Slice 2 WU-2.1: Exercise Domain Models & Drift Tables**:
  - **Domain Layer (`features/exercise/domain/`):**
    - `muscle_group.dart`: Freezed `MuscleGroup` entity, `ExerciseMuscleRef`, and domain enums (`MuscleRole`, `Equipment`, `ExerciseCategory`) matching the backend schema and JSON serialization mappings.
    - `exercise.dart`: Freezed `Exercise` entity with convenience getters (`instructions`, `muscleGroupPrimary`, `muscleGroupsSecondary`, `equipmentType`), default values, and full JSON serialization.
  - **Data Layer (`features/exercise/data/`):**
    - `exercise_tables.dart`: Drift tables `MuscleGroups`, `Exercises`, and `ExerciseMuscleGroups` (with foreign key references, cascade deletion, unique key constraint on `(exerciseId, muscleGroupId)`, and local-first flags `isFavourite`, `isTimeBased`, `isCardio`).
  - **Database Migration & Setup (`core/database/`):**
    - `app_database.dart`: Registered exercise tables in `@DriftDatabase`, bumped `schemaVersion` to 2 with migration step, and enabled `PRAGMA foreign_keys = ON;` in `beforeOpen` for SQLite referential integrity.
  - **Verification & Tests (`test/features/exercise/`):**
    - `exercise_domain_test.dart`: 4 test cases covering domain entity immutability, defaults, copyWith, JSON serialization round-trip, and in-memory Drift SQLite CRUD + cascade delete.
    - `dart run build_runner build` (40 outputs), `flutter analyze` (0 issues), `flutter test` (16/16 passing across the test suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.2: Exercise DAO & Repository** (`ExerciseDao` with search/filter/favourites queries, `ExerciseRepository` interface and Riverpod implementation).
- **2026-08-31 (session 9):** Implemented **Slice 2 WU-2.2 (Exercise DAO & Repository) & WU-2.3 (Exercise Seed Data Asset)**:
  - **Seed Assets (`mobile/assets/data/`):**
    - Extracted 14 muscle groups into `muscle_groups.json` and 55 standard exercises with complete muscle group mappings into `exercises.json` from backend migrations.
    - Registered `assets/data/` in `pubspec.yaml` for bundled zero-network offline operation (Law L2).
  - **Data Layer (`features/exercise/data/`):**
    - `exercise_local_source.dart`: Drift DAO `ExerciseDao` with reactive streams (`watchExercises`, `watchMuscleGroups`, `watchExerciseById`), multi-predicate search (`query`, `category`, `equipment`, `muscleGroupId`, `favouritesOnly`), favourite toggling, transactional custom exercise creation, and batch insertion.
    - `exercise_seed_loader.dart`: Bundled asset loader that parses and seeds SQLite on first launch (guarded by count check or `force` flag).
    - `exercise_repository.dart`: Repository interface `ExerciseRepository` and implementation `ExerciseRepositoryImpl` exposed via `@riverpod` provider.
  - **Database Wiring (`core/database/`):**
    - Registered `ExerciseDao` in `AppDatabase` `daos` array.
  - **Verification & Tests (`test/features/exercise/`):**
    - Created `exercise_dao_test.dart` (6 test cases): seed loading 55 exercises + 14 muscle groups, search query benchmark (<100ms), category & equipment filters, muscle group filtering, favourite status stream reactivity, and custom exercise CRUD.
    - `dart run build_runner build` (40 outputs), `flutter analyze` (0 issues), `flutter test` (22/22 passing across the full suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.4: Exercise List & Search Screen** (`exercise_list_controller.dart`, `exercise_list_screen.dart`, `exercise_search_delegate.dart` with Sharp Glassmorphism UI and filter chips).
- **2026-08-31 (session 10):** Implemented **Slice 2 WU-2.4: Exercise List & Search Screen**:
  - **Presentation Layer (`features/exercise/presentation/`):**
    - `exercise_list_state.dart`: Pure Freezed immutable state model `ExerciseListState` tracking query, filters, muscle groups, exercises, and active filter flags.
    - `exercise_list_controller.dart`: Riverpod `AsyncNotifier` `ExerciseListController` managing reactive debounced search, muscle group/equipment filter chips, favourite toggling, and filter resets.
    - `exercise_search_delegate.dart`: Reusable UI components (`ExerciseSearchBar`, `ExerciseFilterChipsBar`, `ExerciseListTile`) styled with Sharp Glassmorphism design tokens (`AppTheme.oledBlack`, `AppTheme.glassFill`, `AppTheme.glassBorder`, `AppTheme.neonCyan`, `AppTheme.voltGreen`).
    - `exercise_list_screen.dart`: Declarative `ConsumerWidget` with search bar, horizontal filter chips, exercise count header badge, reactive list tiles, and designed empty states (Law L6). Supports both standalone navigation and context-aware selection (`onExerciseSelected`).
  - **Routing (`core/router/app_router.dart`):** Registered `/exercises` route.
  - **Verification & Tests (`test/features/exercise/`):**
    - Created `exercise_list_test.dart` (5 unit/widget tests): verifying controller filter transitions, search debouncing, favourite toggling, screen UI rendering, empty state handling, and "CLEAR ALL FILTERS" action.
    - `dart run build_runner build` (36 outputs), `flutter analyze` (0 issues), `flutter test` (27/27 passing across the full suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.5: Exercise Detail & Custom Exercise Screen** (`exercise_detail_screen.dart`, `create_custom_exercise_screen.dart`).
- **2026-08-31 (session 11):** Implemented **Slice 2 WU-2.5: Exercise Detail & Custom Exercise Screen**:
  - **Presentation Layer (`features/exercise/presentation/`):**
    - `exercise_detail_controller.dart`: Riverpod `AsyncNotifier` `ExerciseDetailController` managing single-exercise reactive lookup, favourite toggling, and custom exercise deletion.
    - `exercise_detail_screen.dart`: Declarative `ConsumerWidget` rendering exercise name, primary/secondary muscle badges, equipment, instructions card, target anatomy card, performance history placeholder, and confirmed custom deletion dialog (Law L7).
    - `create_custom_exercise_controller.dart`: Riverpod `AsyncNotifier` `CreateCustomExerciseController` validating exercise names (uniqueness against local catalog, Law L6) and persisting custom exercises with muscle group associations.
    - `create_custom_exercise_screen.dart`: Complete creation form with primary muscle dropdown, secondary muscle chips, equipment dropdown, category dropdown, time-based/cardio options, and inline error feedback.
  - **Routing (`core/router/app_router.dart`):** Registered `/exercises/new` and `/exercises/:id` routes; connected tile tapping and "+" FAB in `ExerciseListScreen`.
  - **Verification & Tests (`test/features/exercise/`):**
    - Created `exercise_detail_and_custom_test.dart` (4 unit/widget tests): verifying detail loading, favourite toggling, custom exercise validation (empty/duplicate errors), form inputs, and custom creation.
    - `dart run build_runner build` (44 outputs), `flutter analyze` (0 issues), `flutter test` (31/31 passing across the full suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.6: Routine Domain Models & Drift Tables** (`Routine` and `RoutineExercise` Freezed entities, `Routines` and `RoutineExercises` Drift tables in `features/routine/`).
- **2026-08-31 (session 12):** Implemented **Slice 2 WU-2.6: Routine Domain Models & Drift Tables**:
  - **Protocol Update (`AGENTS.md`):** Added explicit "No Redundant Implementation Plans Rule" confirming implementation plans are already established in `EXECUTION_PLAN.md`, `FEATURES.md`, and `ARCHITECTURE.md`.
  - **Domain Layer (`features/routine/domain/`):**
    - `routine_set.dart`: Freezed `RoutineSet` entity representing a planned set in a routine exercise (`id`, `routineExerciseId`, `position`, `setType`, `targetReps`, `targetWeightKg`, `targetRpe`, JSON serialization).
    - `routine_exercise.dart`: Freezed `RoutineExercise` entity (`id`, `routineId`, `exerciseId`, `orderIndex`, `restSeconds`, `notes`, `targetSetsCount`, `targetWeightKg`, `targetReps`, `targetRpe`, `sets`, `exercise` embedding, convenience getters: `position`, `setsCount`, `targetSummary`, JSON serialization).
    - `routine.dart`: Freezed `Routine` entity (`id`, `name`, `description`, `userId`, `exercises`, convenience getters: `exerciseCount`, `totalSets`, `estimatedDurationSeconds`, `estimatedDurationMinutes`, JSON serialization).
  - **Data Layer & Database Schema (`features/routine/data/` & `core/database/`):**
    - `routine_tables.dart`: Drift tables `Routines` (`RoutineRow`), `RoutineExercises` (`RoutineExerciseRow` with foreign key cascade delete on routine/exercise and unique `(routineId, orderIndex)`), and `RoutineSets` (`RoutineSetRow` with foreign key cascade delete and unique `(routineExerciseId, position)`).
    - `app_database.dart`: Registered `Routines`, `RoutineExercises`, `RoutineSets` in `@DriftDatabase`, bumped `schemaVersion` to 3, and added v3 migration block.
  - **Verification & Tests (`test/features/routine/`):**
    - Created `routine_domain_test.dart` (4 unit/integration tests): verifying domain immutability, duration/set count math, JSON serialization roundtrip, in-memory Drift SQLite CRUD, and cascade deletion.
    - `dart run build_runner build` (82 outputs), `flutter analyze` (0 issues), `flutter test` (35/35 passing across the full suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.7: Routine DAO & Repository** (`RoutineDao` with reactive streams `watchAllRoutines()`, `getRoutineWithExercises(id)`, CRUD, and `RoutineRepository` Riverpod implementation).
- **2026-08-31 (session 13):** Implemented **Slice 2 WU-2.7: Routine DAO & Repository**:
  - **Data Layer (`features/routine/data/`):**
    - `routine_local_source.dart`: Drift DAO `RoutineDao` with reactive joins (`watchAllRoutines()`, `getAllRoutines()`, `watchRoutineById()`, `getRoutineById()`), multi-table transactional mutations (`createRoutine`, `updateRoutine`, `deleteRoutine`, `addExerciseToRoutine`, `removeExerciseFromRoutine`), two-pass constraint-safe reordering (`reorderExercises`), and non-destructive duplication (`duplicateRoutine`, Law L7).
    - `routine_repository.dart`: Interface `RoutineRepository` and production implementation `RoutineRepositoryImpl` with domain mappings and `@riverpod` provider.
  - **Database Wiring (`core/database/`):**
    - Registered `RoutineDao` in `AppDatabase` `daos` array.
  - **Verification & Tests (`test/features/routine/`):**
    - Created `routine_dao_and_repo_test.dart` (4 comprehensive tests): in-memory Drift DAO creation & queries, exercise reordering with unique constraint safety, non-destructive cloning (Law L7), and repository domain mapping/CRUD/stream reactivity.
    - `dart run build_runner build` (72 outputs), `flutter analyze` (0 issues), `flutter test` (39/39 passing across the full suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.8: Routines List Screen** (`routine_list_controller.dart`, `routine_list_screen.dart` with Sharp Glassmorphism design and designed empty state per L6).
- **2026-08-31 (session 14):** Implemented **Slice 2 WU-2.8: Routines List Screen**:
  - **Presentation Layer (`features/routine/presentation/`):**
    - `routine_list_controller.dart`: Riverpod `AsyncNotifier` `RoutineListController` streaming all routines, supporting deep-clone duplication, deletion, and fast session startup (`startWorkoutFromRoutine`, pre-populating target sets into SQLite in <1s, Law L1, L7).
    - `routine_list_screen.dart`: Declarative `ConsumerWidget` with Sharp Glassmorphism UI, search filter bar, routine preview cards with exercise count/sets/duration badges (`AppTheme.num` tabular figures), START WORKOUT action, overflow actions (edit, duplicate, delete confirmation dialog), and designed empty state (Law L6).
    - `routines_screen.dart`: Updated to delegate directly to `RoutineListScreen`.
  - **Verification & Tests (`test/features/routine/`):**
    - Created `routine_list_test.dart` (4 comprehensive unit & widget tests): verifying session startup with preloaded targets in <1s, duplication, empty state rendering, search filtering, and action triggers.
    - `dart run build_runner build` (82 outputs), `flutter analyze` (0 issues), `flutter test` (43/43 passing across the full suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.9: Routine Builder / Editor Screen** (`routine_editor_controller.dart`, `routine_editor_screen.dart`, `routine_exercise_editor_sheet.dart`).
- **2026-08-31 (session 15):** Implemented **Slice 2 WU-2.9: Routine Builder / Editor Screen**:
  - **Presentation Layer (`features/routine/presentation/`):**
    - `routine_editor_state.dart`: Freezed UI state `RoutineEditorState` tracking routine metadata, ordered routine exercises, duration/set calculations, and validation.
    - `routine_editor_controller.dart`: AutoDispose Riverpod controller managing routine mutations: adding exercises from library, updating exercise targets, two-pass reordering, deletion with confirmation (Law L7), and synchronous SQLite write-through save.
    - `routine_exercise_editor_sheet.dart`: Sharp Glassmorphism bottom sheet for tuning sets count (steppers), target weight (kg), target reps, rest timer chips (30s, 60s, 90s, 120s, 180s), target RPE, and exercise notes.
    - `routine_editor_screen.dart`: Full-screen builder supporting both creation (`/routines/new`) and editing (`/routines/:id/edit`), with summary metrics cards, drag-and-drop `ReorderableListView`, exercise catalog picker connection, and sticky save bar.
  - **Routing (`core/router/app_router.dart`):**
    - Registered routes: `/routines/new`, `/routines/:id`, and `/routines/:id/edit`.
  - **Verification & Tests (`test/features/routine/`):**
    - Created `routine_editor_test.dart` (5 comprehensive unit & widget tests): verifying empty new routine state, existing routine loading/mutation, exercise reordering and removal, empty name validation, `RoutineExerciseEditorSheet` target configuration, and screen form interactions.
    - `dart run build_runner build` (76 outputs), `flutter analyze` (0 issues), `flutter test` (49/49 passing across the full mobile test suite).
  - **NEXT STEP (exact):** Proceed to **Slice 2 WU-2.10: Routine Detail & Start Workout** (`routine_detail_screen.dart`, `routine_detail_controller.dart`).
- **2026-08-31 (session 16):** Implemented **Slice 2 WU-2.10: Routine Detail & Start Workout** and completed **Slice 2: Exercises & Routines**:
  - **Presentation Layer (`features/routine/presentation/`):**
    - `routine_detail_controller.dart`: Riverpod `AutoDisposeFamilyAsyncNotifier` `RoutineDetailController` streaming routine changes reactively from SQLite, with instant workout creation (`startWorkout`, pre-populating planned sets and target weights/reps into SQLite in <1s while leaving the routine 100% unmutated per Law L7), non-destructive duplication, and cascade deletion.
    - `routine_detail_screen.dart`: Read-only Sharp Glassmorphism UI detail screen showing metadata header, summary cards (Exercises, Sets, Estimated Duration), full set-by-set target tables (`SET | TARGET WEIGHT | REPS | REST`), notes container, sticky "START WORKOUT" action bar, edit routing, and not-found state.
  - **Routing (`core/router/app_router.dart`):**
    - Updated `/routines/:id` route to `RoutineDetailScreen(routineId: ...)`.
  - **Verification & Tests (`test/features/routine/` & `backend/`):**
    - Created `routine_detail_test.dart` (4 comprehensive unit & widget tests): verifying start-workout session creation, immutability of source routine (Law L7), duplicate/delete operations, and UI table rendering.
  - **Slice 2 Status:** ✅ **100% COMPLETE** across full vertical stack (Exercise Catalog, Custom Exercises, Favourites, Routine Tables, DAO, Repository, Routine List, Routine Builder/Editor, and Routine Detail/Start Workout).
- **2026-08-31 (session 17):** Implemented **Slice 3 WU-3.1: Session-Exercise Data Layer**:
  - **Domain Layer (`features/workout/domain/`):**
    - `session_exercise.dart`: Freezed entity `SessionExercise` (`id`, `sessionId`, `exerciseId`, `exercise`, `exerciseName`, `orderIndex`, `restSeconds`, `notes`, `sets`, convenience getters: `position`, `setsCount`, `completedSetsCount`, `totalVolumeKg`, JSON serialization).
    - `workout_set.dart` & `workout_session.dart`: Updated to include `sessionExerciseId`, set metadata (`completedAt`, `isPr`, `notes`), and aggregate calculations (`totalSetsCount`, `completedSetsCount`, `totalVolumeKg`).
  - **Data Layer & Database Schema (`features/workout/data/` & `core/database/`):**
    - `workout_tables.dart`: Added `SessionExercises` table (`SessionExerciseRow` with foreign key cascade delete on session and exercise, and unique `(sessionId, orderIndex)`) and updated `WorkoutSessions` and `WorkoutSets` tables (`WorkoutSetRow` with foreign key on `sessionExerciseId` and unique `(sessionExerciseId, setNumber)`).
    - `app_database.dart`: Registered `SessionExercises` table in `@DriftDatabase`, bumped `schemaVersion` to 4, and added v4 migration step.
    - `workout_local_source.dart`: Updated `WorkoutDao` with joined reactive queries (`watchSessionExercisesWithSets`, `watchSessionWithExercises`), carrier models (`SessionWithExercises`, `SessionExerciseWithSets`), transactional two-pass collision-safe reordering (`reorderSessionExercises`), and cascade deletion.
    - `workout_repository.dart`: Updated `WorkoutRepository` interface and `WorkoutRepositoryImpl` with domain entity mapping and Riverpod provider.
  - **Presentation Layer Updates (`features/workout/presentation/` & `features/routine/presentation/`):**
    - `active_workout_state.dart` & `active_workout_controller.dart`: Updated state and controller with optimistic `SessionExercise` updates and write-through persistence.
    - `routine_detail_controller.dart` & `routine_list_controller.dart`: Updated `startWorkout` to create `SessionExercises` and link target sets via `sessionExerciseId`.
  - **Verification & Tests (`test/features/workout/` & `backend/`):**
    - Created `session_exercise_test.dart` (5 comprehensive unit & DAO tests): testing domain immutability/volume math, JSON serialization, in-memory Drift SQLite session exercises CRUD, two-pass reordering, cascade set deletion, and routine startup.
    - Updated `workout_reference_slice_test.dart` with `sessionExerciseId` and SQLite setup.
- **2026-08-31 (session 18):** Implemented **Slice 3 WU-3.2: Ghost Prefill Logic**:
  - **Domain Layer (`features/workout/domain/`):**
    - `ghost_set.dart`: Freezed value object `GhostSet` (`weightKg`, `reps`, `rpe`, `setType`, `source`, with `hasValue`, `weightDisplay`, `repsDisplay`, `prevSummary` helpers and JSON serialization) and `GhostSource` enum (`history`, `routineTarget`, `previousSet`, `none`).
  - **Data Layer & Repository (`features/workout/data/`):**
    - `workout_local_source.dart` & `workout_repository.dart`: Added `getLastCompletedSetsForExercise(exerciseId)` executing <10ms indexed SQLite lookup on completed workouts.
    - `ghost_prefill_service.dart`: Created `GhostPrefillService` and Riverpod provider implementing the standard set population & ghost value fallback logic from `FEATURES.md` §7.2:
      1. *History match:* When $N \le \text{historical count}$, prefill from historical Set $N$.
      2. *Active session previous:* When $N > 1$ and Set $N-1$ exists in current session, copy from Set $N-1$.
      3. *History overflow fallback:* When $N > \text{historical count}$ and history exists, fall back to last historical set.
      4. *Routine target:* Prefill from routine target sets or per-exercise target configuration.
      5. *Empty fallback:* Return clean empty ghost (`hasValue: false`) when no prior history or targets exist.
      - Includes batch resolution `resolveGhostSetsForExercise` for instant exercise block rendering.
  - **Verification & Tests (`test/features/workout/` & `backend/`):**
    - Created `ghost_prefill_test.dart` (7 comprehensive unit tests) covering domain value object formatting/serialization, all 5 resolution branches, and batch resolution.
- **2026-08-31 (session 19):** Implemented **Slice 3 WU-3.3: Exercise Picker (In-Session Mode)**:
  - **Data Layer & Repository (`features/workout/data/`):**
    - `workout_local_source.dart` & `workout_repository.dart`: Added `getRecentExercises(limit: 10)` to query distinct exercises used across completed workout history in SQLite.
  - **Presentation Layer (`features/workout/presentation/` & `core/router/`):**
    - `exercise_picker_state.dart`: Freezed state `ExercisePickerState` (`searchQuery`, `selectedMuscleGroupId`, `selectedEquipment`, `favouritesOnly`, `showRecentOnly`, `recentExercises`, `allExercises`, `muscleGroups`, with computed `filteredExercises` and `hasActiveFilters`).
    - `exercise_picker_controller.dart`: Auto-dispose Riverpod controller managing search filtering, filter chips toggling, recent history query, and optimistic favourites toggling with SQLite persistence.
    - `exercise_picker_screen.dart`: Full-screen Sharp Glassmorphism modal (`/workout/picker`) with `ExerciseSearchBar`, horizontal chip filter bar (`⚡ Recent (N)`, `★ Favourites`, muscle groups, equipment), count badge, `ExerciseListTile` items, callback/active session selection handling, and designed empty states (Law L6) with "CREATE CUSTOM" CTA.
    - `app_router.dart`: Registered `/workout/picker` route.
  - **Verification & Tests (`test/features/workout/` & `backend/`):**
    - Created `exercise_picker_test.dart` (8 comprehensive unit & widget tests) verifying search query filtering (<300ms latency), filter combinations, controller recent history SQLite queries, optimistic favourites toggles, and modal tap-to-add callbacks.
- **2026-08-31 (session 20):** Implemented **Slice 3 WU-3.4: Active Workout Screen Rebuild**:
  - **Controller & Engine Enhancements (`features/workout/presentation/`):**
    - `active_workout_controller.dart`: Added `updateWorkoutName(newName)` (inline session rename with write-through), `updateSet(set)` (optimistic + SQLite write-through), `generateWarmupPyramid(workingWeightKg)` (§8.1 progressive 4-tier warmup ladder: Empty bar 20kg × 10, 50% × 5, 70% × 3, 85% × 1 flagged as `SetType.warmup`), `generateDefaultWorkoutName(time)` (time-of-day auto-naming), and collision-safe monotonic set ID generation.
  - **Presentation Widgets (`features/workout/presentation/widgets/`):**
    - `set_row_widget.dart`: Interactive set row with `SET #` / `W` warmup / `D` dropset badge, `PREV` ghost value preview, `KG` quick steppers (`−2.5` / `+2.5` buttons + edit dialog), `REPS` quick steppers (`−1` / `+1` buttons + edit dialog), and Volt Green `✓` toggle with haptic feedback & rest timer trigger.
    - `warmup_pyramid_sheet.dart`: Bottom modal calculating and previewing warmup ladder with target working load adjustment.
    - `exercise_block_card.dart`: Dynamic exercise card block with muscle tag, 3-dots popup menu (Warm-up pyramid, Remove exercise), table headers, set rows list, and `+ ADD SET` button.
  - **Active Workout Screen Rebuild (`features/workout/presentation/active_workout_screen.dart`):**
    - Auto-named / editable title header with duration and summary stats (`SETS`, `WORKING VOLUME` strictly excluding warmups per Law L1, `EXERCISES`).
    - Integrated `GhostPrefillService` batch resolution for instant rendering of previous performance values.
    - Sticky bottom rest timer bar (`REST TIMER: MM:SS`, `+15s` button, stop/skip `✕` button).
    - Sticky bottom action bar (`[+ ADD EXERCISE]`, `[✓ FINISH]`) and cancel/discard confirmation dialog.
  - **Verification & Tests (`test/features/workout/` & `backend/`):**
    - Created `active_workout_screen_test.dart` (5 comprehensive unit & widget tests) verifying time-of-day auto-names, warmup pyramid generation & volume exclusion, set/session mutation, and widget rendering & stepper interactions.
    - `dart run build_runner build` (102 outputs), `flutter analyze` (0 issues), `flutter test` (**80/80 tests passing across mobile**), `./gradlew test` (**BUILD SUCCESSFUL across backend**).
  - **NEXT STEP (exact):** Proceed to **Slice 3 WU-3.5: Rest Timer & Notifications** (`rest_timer_service.dart`, background audio/vibration notification on timer completion, notification action `+15s`, lock-screen synchronization).
- **2026-08-31 (session 21):** Implemented **Slice 3 WU-3.5: Rest Timer & Notification**:
  - **Notification Infrastructure (`core/notifications/`):**
    - `notification_service.dart`: `flutter_local_notifications` 22.3.0 added (v22 named-param API). Silent low-importance `avenfit_rest_timer` channel (§8.3 — no sound/vibration; completion feedback is a P1 Settings concern), idempotent `initialize()`, Android 13+ `POST_NOTIFICATIONS` runtime prompt, session-latched just-in-time priming (grant / "not now" — never re-nagged, §3), `showRestCountdown` (updates in place with progress bar + "+15s" action), `showRestComplete`, and `cancelRestNotification`. Every platform call is guarded — notifications denied or unsupported platforms never break the rest flow (L2/L4). `POST_NOTIFICATIONS` declared in `AndroidManifest.xml`.
    - keepAlive `notificationServiceProvider`; channel bootstrap fires once on first watch. Full lock-screen action delivery is deferred to the WU-3.10 foreground service (the "+15s" action currently works while the app process is alive in the foreground).
  - **Rest Timer Engine (`features/workout/presentation/`):**
    - `rest_timer_state.dart`: Freezed `RestTimerState` — epoch deadline `endsAtEpochMs` is the single source of truth, `initialSeconds` preserves restart semantics, plus MM:SS / progress-fraction helpers.
    - `rest_timer_controller.dart`: keepAlive `RestTimerController` — auto-starts on set confirmation (default 90s, per-exercise `restSeconds` override), `Stream.periodic` used ONLY for UI/notification refresh ticks with every value recomputed from the deadline (L8 — zero drift, zero CPU polling), +15s/−15s applied to the epoch deadline (dropping ≤0 dismisses), restart restores the original duration, instant cancel (§8.3 mutual exclusivity: resting ≠ lifting — any ✓ replaces the countdown), warm-up sets cancel rest instead of starting it, and natural completion flips the notification to a silent "rest complete" notice.
    - `widgets/rest_timer_bar.dart`: slim animated bar under the session header — remaining time, ±15s, restart, dismiss, cyan progress track; never blocks content (§8.1/§8.3).
  - **Integration (`active_workout_controller.dart` / `active_workout_screen.dart` / `active_workout_state.dart`):**
    - Removed the legacy `Timer.periodic` rest logic and the duplicated rest fields from the workout controller/state; `toggleSetCompleted` now delegates to `RestTimerController`, and `finishWorkout`/`cancelWorkout` clear the countdown.
    - Screen renders `RestTimerBar` under the header (AnimatedSize in/out), adds a header timer button for manual rest start (§8.3/L1), and shows the explainer primer dialog ("See your rest countdown on the lock screen") before the OS prompt on the first rest start.
  - **Verification & Tests (`test/features/workout/`):**
    - Created `rest_timer_test.dart` (13 tests): epoch deadline math, ±15s symmetry, dismissal below zero, cancel, restart semantics, replacement (auto-cancel rule), manual start, fake-async tick/complete pipeline, JIT primer grant/deny/no-re-nag, and `RestTimerBar` widget interactions.
    - Created `fake_notification_service.dart` shared test double; updated `workout_reference_slice_test.dart` and `active_workout_screen_test.dart` to the new rest-timer API.
    - `dart run build_runner build` (117 outputs), `flutter analyze` (0 issues), `flutter test` (**93/93 passing across mobile**).
  - **NEXT STEP (exact):** Proceed to **Slice 3 WU-3.6: PR Detection Engine** (`features/progress/domain/pr_record.dart`, `pr_tables.dart`, `pr_repository.dart` — 4 PR types, warm-up exclusion, Epley 1RM, hooked into set confirmation) — parallel-safe with WU-3.7/WU-3.8.
- **2026-08-31 (session 22):** Implemented **Slice 3 WU-3.6: PR Detection Engine**:
  - **Domain Layer (`features/progress/domain/` — new feature tree):**
    - `pr_record.dart`: Freezed `PRRecord` entity (`id`, `exerciseId`, `recordType`, `value`, `weightKg` weight-key context for reps-at-weight records, `achievedAt`, `sessionId`, `setId`, timestamps, JSON serialization) + `RecordType` enum (`maxWeight`, `epley1rm`, `maxRepsAtWeight`, `volume`) with display labels and tolerant column parsing, and the Epley static `epleyE1RM = weight × (1 + reps / 30)` with zero-value guards.
  - **Data Layer (`features/progress/data/`):**
    - `pr_tables.dart`: Drift `PersonalRecords` table (`PersonalRecordRow`) — one row per (exercise, recordType), plus one per weight for the weight-keyed `maxRepsAtWeight` type (uniqueness maintained by code-managed upsert since SQLite treats NULLs as distinct in unique keys). `exerciseId`/`sessionId`/`setId` FK-cascade so deleting a set/session/exercise can never orphan PR rows (L7).
    - `pr_local_source.dart`: `PrDao` (renamed from `PRDao` — drift's camelCase getter would otherwise be `pRDao`) with record lookups (NULL-aware `findRecord` for weight-keyed vs weight-less types), insert/update, per-exercise clear, and `getReplayableWorkingSets` (completed, non-warmup, non-discarded-session sets ordered oldest→newest for recompute replay).
    - `pr_repository.dart`: `PRRepository` contract + `PRRepositoryImpl` + `@riverpod` provider. `detectPRs(exerciseId, set)` — guards (unconfirmed/warm-up/0-weight/0-reps never count, §10.2), computes the 4 candidates (max weight, Epley e1RM, reps-at-weight keyed by exact weight, per-set volume = weight × reps), upserts each strictly-greater beat in place (equal values keep the earlier date), and returns the achieved `PRRecord` list. `recomputePRs(exerciseId)` — transactional wipe + chronological replay through the same detection math (detection == recompute parity, the self-healing path for set edits/deletes).
  - **Database (`core/database/`):** `PersonalRecords` registered in `@DriftDatabase` + `PrDao` in `daos`; `schemaVersion` 4 → 5 with `from < 5` migration creating `personal_records`.
  - **Controller Hook (`features/workout/presentation/active_workout_controller.dart`):** `toggleSetCompleted` now runs PR detection after the optimistic ✓ update (instant local lookup, no network), flags `isPr` on the set (state re-emission + single write-through via new `_withSet` state-patch helper), and delegates rest-timer logic unchanged. Warm-ups are excluded at both controller and engine level.
  - **PR Badge (`widgets/set_row_widget.dart`):** `_PrFlashBadge` — tiny volt-green "PR" chip stacked under the set-number badge, pops in with scale/fade (TweenAnimationBuilder) and fires a single medium haptic on mount (L5: haptic + flash, no sounds/confetti). Celebration haptic lives in the widget layer, keeping the controller platform-free.
  - **Dependency note:** `json_annotation` floor raised `^4.9.0` → `^4.12.0` (resolved version unchanged at 4.12.0) to satisfy json_serializable's minimum-constraint check (build warning).
  - **Verification & Tests (`test/features/progress/pr_detection_test.dart` — 16 tests):** Epley formula exactness + zero guards + tolerant enum parse; first-set detection of all 4 types; heavier-set beats; equal/lower no-PR (strictly-greater rule); volume-beats-weight case (50×20); warm-up exclusion even above all records; unconfirmed-set exclusion; per-weight reps record keying; achievement source-id round-trip through real FK rows; recompute parity (replay reproduces identical records) and stale-record drop after deleting the PR set (L7); controller hook integration (isPr flagged in state + SQLite, warm-ups never flag, in-place record updates) with `appDatabaseProvider`/`prRepositoryProvider` overrides; PR badge widget show/hide.
    - Fixed the pre-existing test containers in `workout_reference_slice_test.dart` and `active_workout_screen_test.dart` (5 `ProviderContainer`s) to override `appDatabaseProvider` — required now that the controller depends on `prRepositoryProvider` (previously they fell through to a real database open).
    - `dart run build_runner build` (clean, no warnings), `flutter analyze` (0 issues), `flutter test` (**109/109 passing across mobile** — was 93).
  - **NEXT STEP (exact):** Proceed to **Slice 3 WU-3.7: Workout Summary Screen** (`workout_summary_screen.dart` + `workout_summary_controller.dart` — stats grid incl. PR count via `WorkoutSet.isPr`/PR table, exercise breakdown, streak badge, haptic PR celebration, "Done" → Home; depends on WU-3.4 ✓ + WU-3.6 ✓). WU-3.8 (crash resilience & session restore) remains parallel-safe afterwards.
- **2026-08-31 (session 23):** Implemented **Slice 3 WU-3.7: Workout Summary Screen** (FEATURES.md §8.5) and committed the previously-pending WU-3.6 working tree as its own conventional commit first (`b64c177`).
  - **Data Layer additions (`features/workout/data/`):**
    - `workout_local_source.dart` + `workout_repository.dart`: `renameSession(sessionId, name)` (inline rename write-through, L7) and `getCompletedWorkoutCountSince(since)` (`selectOnly` + `countAll()` — basis of the weekly streak badge).
  - **Presentation Layer (`features/workout/presentation/`):**
    - `workout_summary_state.dart`: Freezed `WorkoutSummaryState` — nullable session (drives the designed not-found state), per-exercise blocks, weekly count/goal; computed getters: epoch-math `durationSeconds` (stored `durationSeconds` first, else `completedAt − startedAt`, L8), `completedSetsCount`, `totalVolumeKg` (warm-ups excluded via `SessionExercise.totalVolumeKg`, L1), `prCount` (completed sets with `isPr`), `weeklyGoalMet`. `kDefaultWeeklyGoal = 3` (§5.2 default) until WU-X.2 adds streak settings.
    - `workout_summary_controller.dart`: AutoDispose family AsyncNotifier keyed by sessionId — loads the fully persisted session via a single joined `getSessionById` (no re-fetch, no network, L2/L7) + weekly count from Monday 00:00; `renameWorkout` optimistic update + write-through, blank submissions fall back to 'Workout' (§8.5).
    - `workout_summary_screen.dart`: Sharp Glassmorphism summary — editable title (pencil → rename dialog), completed-date line, conditional volt-green **WEEKLY GOAL MET** streak badge (only when met — never a nag, L4; WU-X.2 will replace with settings-driven streak), 2×2 stats grid (DURATION · WORKING VOLUME · SETS · PRS), `_PrStatCell` with one-shot volt-green flash (TweenAnimationBuilder) + single medium haptic fired via `ref.listen` on first data with PRs (L5 — no confetti/sounds), per-exercise breakdown cards (`weight × reps` rows, warm-ups W-badged, PR chips), `DONE` → `context.go('/home')`, and L6 states: loading spinner, error + RETRY, "WORKOUT NOT FOUND" + BACK TO HOME. Note: Riverpod 3 removed `AsyncValue.valueOrNull` — use `.value`.
  - **Wiring:**
    - `app_router.dart`: registered `/workout/summary/:id`.
    - `active_workout_controller.dart`: `finishWorkout()` now returns `Future<String?>` (completed session id for summary navigation, null on failure); **fixed pre-existing broken `updateWorkoutName`** — it silently never persisted ("await (repository as dynamic)" no-op); now trims + writes through via `renameSession` (L7).
    - `active_workout_screen.dart`: FINISH & SAVE → `finishWorkout()` → `context.go('/workout/summary/$id')` (summary replaces the workout screen per §8.5).
  - **Verification & Tests (`test/features/workout/workout_summary_test.dart` — 12 tests):** stats math (warm-up exclusion, PR count, exercise names), duration stored vs epoch fallback (3930s case), not-found state, rename write-through + 'Workout' blank fallback (with autoDispose keepalive listener — read-mutate-read needs `container.listen`), weekly-goal met/not-met (8-days-back session never counts), widget rendering (stats grid, PR chip, streak badge), streak hidden when goal unmet, inline rename dialog incl. blank fallback, not-found widget state, and DONE → Home navigation via real GoRouter.
    - `dart run build_runner build` (268 outputs written), `flutter analyze` (0 issues), `flutter test` (**121/121 passing across mobile** — was 109).
  - **NEXT STEP (exact):** Proceed to **Slice 3 WU-3.8: Crash Resilience & Session Restore** (synchronous set write-through guarantee, launch-time `is_active` restore with epoch elapsed math, Home resume banner, one-session conflict confirm dialog) — parallel-safe with WU-3.9/WU-3.10.
- **2026-08-31 (session 24):** Implemented **Slice 3 WU-3.8: Crash Resilience & Session Restore** (FEATURES.md §7.1/§8.1, Laws L1/L7/L8):
  - **Schema & Domain (`workout_tables.dart`, `app_database.dart`, `workout_session.dart`):**
    - `WorkoutSessions` gained `isPaused` (bool), `lastResumedAt` (epoch of the last ticked moment — the freeze point while paused, the RN `last_resumed_at` pattern), and `pausedDurationSeconds`; `schemaVersion` 5 → 6 with `m.addColumn` migration.
    - `WorkoutSession.elapsedSecondsNow()` — pure epoch math: running = `now − startedAt − pausedDuration`; paused = frozen at `lastResumedAt`; completed = stored `durationSeconds` (else epoch fallback); discarded frozen; clamped ≥ 0.
  - **Data Layer (`workout_local_source.dart`, `workout_repository.dart`):** `pauseSession/resumeSession` (resume folds the paused span into `pausedDurationSeconds` in a transaction) with `pauseWorkout/resumeWorkout` repo write-through methods + full row→domain mapping of the new fields; new reactive `watchActiveSessionProvider` (joined stream) for the Home banner and rule checks.
  - **Controller (`active_workout_controller.dart`):**
    - `build()` is now the launch-time restore path: active session → exercises + sets + epoch `elapsedSeconds` + `wasRestored: true` (state gained the flag; screen shows a dismissible cyan "WORKOUT RESUMED · MM:SS ELAPSED" banner per §8.1 restored-session state).
    - **`toggleSetCompleted` reordered for L7:** confirmed values write through to SQLite *before* the UI locks the row (J1 gate); PR detection moved after the durable write in try/catch (a PR failure can never lose the set); un-completing now also clears `isPr`.
    - `pauseWorkout()/resumeWorkout()` (write-through, then state) and `finishWorkout()` now stores `durationSeconds` computed by epoch math — the summary never re-derives it.
  - **UI (`active_workout_screen.dart`, `home_screen.dart`):** pause/resume toggle button in the appbar (§8.1 header), ticking ELAPSED stat column (`_ElapsedStatColumn` StatefulWidget — 1s `Timer.periodic` cancelled synchronously in dispose, value always recomputed from epoch fields, frozen while paused, zero CPU polling L8), Home resume banner (§7.1: cyan-bordered "WORKOUT IN PROGRESS · name · MM:SS elapsed · n/m sets", reactive from SQLite, tap → Active Workout).
  - **One-session rule (§8.1):** new `widgets/session_conflict_dialog.dart` — `resolveOneSessionRule()` checks the active session and prompts **Resume / Save as completed / Discard (double confirm, L7)**; wired into Home START NEW SESSION, Routine List START, and Routine Detail START (never silently discards data). Entry dialogs are presentation-only; routine start latency (<1s) preserved.
  - **Verification & Tests (`test/features/workout/session_restore_test.dart` — 21 tests):** epoch elapsed math (running/paused-frozen/resumed/completed-stored/discarded-frozen/negative-clamp), DAO pause/resume with deterministic paused span, controller launch-restore (exact exercises/sets/volume/elapsed + wasRestored), set-confirmation SQLite persistence before UI lock, un-complete PR-flag clearing, controller pause/resume write-through, finish duration epoch math (~300s ±3), conflict-rule widget tests (no-session pass-through, save-as-completed, discard-with-confirm, resume-navigation) and Home banner tests (content, tap-through, absent when no session). Test-harness pattern established: `UncontrolledProviderScope` with an undisposed container so drift's stream-close cleanup timer never trips the pending-timer invariant (also applied to `widget_test.dart` now that Home watches a live SQLite stream).
  - `dart run build_runner build` (161 outputs), `flutter analyze` (0 issues), `flutter test` (**142/142 passing across mobile** — was 121).
  - **NEXT STEP (exact):** Proceed to **Slice 3 WU-3.9: Workout History & Past Workout Detail** (`features/history/` — `watchCompletedWorkouts()` paginated repository, `history_list_screen.dart` grouped by date, `workout_detail_screen.dart` with "Repeat workout" + "Save as routine") — parallel-safe with WU-3.10 (Foreground Service).
- **2026-08-31 (session 25):** Implemented **Slice 3 WU-3.9: Workout History & Past Workout Detail** (FEATURES.md §8.6/§8.7):
  - **Domain (`features/history/domain/workout_history_item.dart`):** Freezed `WorkoutHistoryItem` (`id`, `name`, `date` = completedAt fallback startedAt, `durationSeconds`, `exerciseCount`, `totalSetsCount` (confirmed sets), `totalVolumeKg` (warm-ups excluded, L1), `prCount`, `volumeDisplay` helper).
  - **Data (`features/history/data/`):** No new tables (per plan) — `history_local_source.dart` adds `HistoryDao` (registered in `AppDatabase.daos`) over the workout + routine tables: reactive paginated `watchCompletedWorkouts(limit, offset)` with two batched queries per page (no N+1) and in-memory aggregation; one-shot `getCompletedWorkouts`; transactional `repeatWorkout` (new active session, exercises keep order/rest/notes, every original set copied as a *planned* row — unconfirmed, PR flags cleared, warm-up types preserved; original 100% untouched, L7); transactional `saveWorkoutAsRoutine` (routine + exercises + per-set `RoutineSets` targets mirroring performed weight/reps/RPE); `deleteWorkout` (FK-cascade removes exercises/sets/PR rows) returning affected exercise ids.
  - **Repository (`history_repository.dart`):** `HistoryRepository` contract + `HistoryRepositoryImpl` composing `HistoryDao` + `WorkoutRepository` (joined detail reads) + `PRRepository` — `deleteWorkout` runs `recomputePRs` for every affected exercise so PRs self-heal to the best surviving set (§10.2 parity, L7). Providers: `historyRepositoryProvider`, `HistoryFeedLimit` (page size 50, `loadMore()`), reactive `watchWorkoutHistoryProvider`.
  - **Presentation (`features/history/presentation/`):**
    - `history_list_screen.dart`: full history feed grouped by date buckets (TODAY · YESTERDAY · THIS WEEK · month, manual formatting — no new deps), cards with name, relative date ("2d ago"), exercise count, `sets · volume · duration`, volt-green `N PR` chip, LOAD MORE pagination, designed empty ("NO WORKOUTS YET" → start CTA) / loading / error+RETRY states (L6).
    - `workout_detail_screen.dart` + `workout_detail_controller.dart`: read-only past-workout view mirroring the Workout Summary minus celebrations — editable title (rename write-through), COMPLETED date line, 2×2 stats grid (DURATION · WORKING VOLUME · SETS · PRS), per-exercise breakdown cards (`weight × reps`, W-badged warm-ups, PR chips, "(not completed)" planned rows), sticky **REPEAT WORKOUT** bar (goes through `resolveOneSessionRule` §8.1), overflow: **Save as routine** (name dialog → snackbar) and **Delete** (double confirm, L7 → pops back). Not-found state with BACK TO HISTORY (L6).
  - **Wiring:** routes `/history` + `/history/:id`; the Progress-tab placeholder now exposes a WORKOUT HISTORY button (temporary until WU-X.3 builds the dashboard).
  - **Verification & Tests (`test/features/history/history_test.dart` — 18 tests):** newest-first ordering with exact aggregates (warm-up/planned exclusions, PR count, stored duration), limit/offset pagination, reactive re-emission on completion, full detail read, repeat copies (planned sets, original untouched, active-session restore path), save-as-routine per-set targets, empty-workout rejection, delete with PR self-heal (110 kg record reverts to 100 kg after delete), list screen empty/cards/PR-chip/navigation, detail stats+breakdown+not-found+repeat-through-conflict-rule+save-as-routine+delete-confirm.
  - `dart run build_runner build` (168 outputs), `flutter analyze` (0 issues), `flutter test` (**160/160 passing across mobile** — was 142).
  - **NEXT STEP (exact):** Proceed to **Slice 3 WU-3.10: Foreground Service (Active Session)** — native Kotlin service in `android/app/src/main/` (persistent notification: elapsed time + rest countdown + +15s/Finish actions, Android 14+ service type compliance), `core/services/workout_foreground_service.dart` platform-channel bridge, wired into `ActiveWorkoutController` (start on session create, update on set log/rest timer, stop on finish) and the `notification_service.dart` action handler. WU-X.2 (forgiving weekly streak) remains parallel-safe.
- **2026-08-31 (session 26):** Implemented **Slice 3 WU-3.10: Foreground Service (Active Session)** (FEATURES.md §8.1, L8) — native Android + platform channel:
  - **Native (`android/app/src/main/kotlin/com/avenfit/aven_fit/`):** new `WorkoutForegroundService.kt` — persistent foreground notification during active sessions showing elapsed time and, when resting, the live rest countdown, re-rendered every second by a main-looper Handler from pure epoch fields (`startedAtMs`, `isPaused`, `pausedDurationMs`, `lastResumedAtMs`, `restEndsAtMs`) — zero accumulation drift (L8). Dart is the single source of truth; actions mirror app state: [+15s] appears only while resting (optimistic native epoch extend + best-effort forward to Dart) and [Finish Workout] forwards to Dart (write-through completion, L7). `startForeground` uses `FOREGROUND_SERVICE_TYPE_SPECIAL_USE` on API 34+ (Android 14+ compliance); dedicated silent low-importance channel `avenfit_workout_service`; content tap relaunches the app; `START_NOT_STICKY` (Dart re-pushes state on restore).
  - **Manifest:** `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_SPECIAL_USE` permissions; service declared `exported="false"` with `foregroundServiceType="specialUse"` and the `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` workout-timer property.
  - **Channel bridge (`MainActivity.kt` + `core/services/workout_foreground_service.dart`):** single `com.avenfit.aven_fit/workout_service` MethodChannel, both directions — Dart → native `startSession`/`updateSession` (absent extras keep native values)/`updateRest` (`-1` sentinel clears)/`stop`; native → Dart `onAdd15`/`onFinish` via a companion-held channel reference (best-effort; a dead engine just keeps ticking epoch values). The Dart bridge is gated on `Platform.isAndroid` (short-circuits on desktop test hosts — no dangling channel replies) with a `debugForceSupported` test seam, and all calls are guarded no-ops (L2). `updateSession` carries workoutName → rename updates the notification title.
  - **Wiring:** `ActiveWorkoutController` pushes start on session create **and** launch-time restore, updates on pause/resume/rename, stops on finish/discard. `RestTimerController` (the keep-alive session-scope component) owns the notification actions: `onAdd15 → addTime(15)`, `onFinish → _finishFromNotification` (completes the active session via repository write-through + epoch duration, stops the service, clears notifications) — so Finish works from the lock screen even from the Home tab; rest deadlines are pushed on start/±adjust/cancel/complete. The existing `flutter_local_notifications` countdown notification + its action handler (WU-3.5) remain untouched alongside the service notification.
  - **Verification:** `gradlew :app:compileDebugKotlin :app:processDebugMainManifest` → BUILD SUCCESSFUL (Kotlin compiles, manifest merges). New `test/features/workout/foreground_service_test.dart` (10 tests) with a recording `FakeWorkoutForegroundService`: rest deadline mirroring (start/±15/cancel → pushed deadlines; `-1` clear sentinel verified against a mock channel), `+15s`/`Finish` notification actions (session completed with epoch duration + service stopped; no-active-session path stops only), lifecycle mirroring (start/pause/resume/rename/finish/discard/launch-restore), platform gate (no channel traffic on the test host). Hardening found during testing: unmocked platform channels *hang* the flutter-test harness, so the bridge gate + lazy handler registration (behind the gate) are load-bearing for the whole suite.
  - `dart run build_runner build` (174 outputs), `flutter analyze` (0 issues), `flutter test` (**170/170 passing across mobile** — was 160).
  - **NEXT STEP (exact):** Proceed to **WU-X.1: Home Dashboard Rebuild** (all dependencies now met) and **WU-3.11 Slice 3 test sweep**.
- **2026-08-31 (session 27):** Implemented **WU-X.2: Forgiving Weekly Streak** (FEATURES.md §17 / §5.2, Laws L4/L7/L8):
  - **Domain (`features/progress/domain/streak_info.dart`):** Freezed `StreakInfo` (`weeklyGoalDays`, `workoutsThisWeek`, `currentStreakWeeks` + `weeklyGoalMet`, `weeklyProgressDisplay` ("2 of 4"), `streakDisplay` helpers) and the pure `StreakInfo.compute()` engine: Monday-anchored weeks via the single `StreakInfo.startOfWeek` anchor; a week meeting the goal extends the chain; the **in-progress current week never breaks the chain mid-week** (judged at last week until met — L4); a *lapsed* week below goal resets to 0; a met current week starts a new chain after a miss; weeks before the first-ever workout are pre-history, not misses. `kDefaultWeeklyGoal = 3` now lives here (moved from `workout_summary_state.dart`, which re-exports it).
  - **Data (`features/progress/data/`):** `streak_tables.dart` — `StreakSettings` singleton row (id `'default'`, `weeklyGoalDays` default 3 as a **literal**: drift_dev crashes analyzing non-literal const column defaults); schemaVersion 6 → 7 with `createTable(streakSettings)` migration. `streak_local_source.dart` — `StreakDao`: settings upsert (insert+update paths), completed-dates read (status `'completed'`, effective date = `completedAt ?? startedAt`), and `watchStreakInput()` — the drift-native dual-trigger stream: a `customSelect` subselect of the goal with `readsFrom: {streakSettings, workoutSessions}` + `.watchSingle()` re-emits when **either** table changes, re-reading dates fresh per emission (L8, zero polling; note `watch()` on a `Selectable` returns `Stream<List<QueryRow>>` — `watchSingle()` for aggregate queries). `streak_repository.dart` — `StreakRepository` contract + impl + `streakRepositoryProvider` + reactive `watchStreakInfoProvider`. The streak is **never persisted** — re-derived from workout history on every read so edits/deletes self-heal (L7).
  - **Wiring:** `WorkoutSummaryController` now reads goal + weekly count through `StreakRepository` (the hardcoded `kDefaultWeeklyGoal` assumption is gone; the controller's private `_startOfWeek` deleted — one week-anchor everywhere); dead `getCompletedWorkoutCountSince`/`getCompletedSessionCountSince` removed from the workout repository + DAO. `HomePage` gained the §5.2 glance stats row (`_GlanceStatsRow`/`_GlanceChip`): "THIS WEEK · X of Y" glass chip (cyan when met) + "STREAK · N week(s)" chip **omitted entirely at 0** (fact, never a shaming verdict — L4). The full Home rebuild remains WU-X.1.
  - **Tests:** new `test/features/progress/streak_test.dart` (21 tests): 10 pure-math (incl. the plan's verification cases — 2 met weeks → streak 2, missed week → 0 — plus mid-week grace, partial lapsed week, same-day multi-session, goal fallback), 5 DAO/repo (defaults, upsert both paths, effective-completion-week counting, active/discarded exclusion, dual-trigger watch re-emission), 4 Home glance widget states, 2 summary-controller wiring. `workout_summary_test.dart`: all 12 containers override `streakRepositoryProvider` (they don't override `appDatabaseProvider` — the streak provider must not fall through to a real DB open) and the weekly-count test is weekStart-anchored (deterministic on any weekday; the streak counts by effective completion date).
  - `dart run build_runner build` (clean), `flutter analyze` (0 issues), `flutter test` (**191/191 passing across mobile** — was 170).
  - **NEXT STEP (exact):** Proceed to **WU-X.1: Home Dashboard Rebuild** (`features/workout/presentation/home_screen.dart` + `home_controller.dart` — greeting, resume banner, START, suggested routine, glance stats (streak now exists), calories-remaining (hidden without goals, L4), recent 10 workouts), then **WU-3.11 Slice 3 test sweep**.
- **2026-08-31 (session 28):** Implemented **WU-3.11: Slice 3 Comprehensive Tests** — **Slice 3 is now ✅ COMPLETE**. User instruction recorded (taste): work sequentially per EXECUTION_PLAN.md, defer cross-cutting WUs (WU-X.1/X.3/X.4/X.5) until later, wait for explicit go-ahead per WU.
  - **Coverage audit vs the WU-3.11 checklist:** `ghost_prefill_test.dart` ✓ (7 tests, all 4 branches + batch), `pr_detection_test.dart` ✓ (16 tests, 4 PR types/warm-up exclusion/Epley/recompute parity), `session_exercise_test.dart` ✓ (5 tests, add/reorder/cascade), `workout_reference_slice_test.dart` ✓ (updated for v4 schema in WU-3.1). The one gap: the plan's `session_lifecycle_test.dart` did not exist — pause/resume/finish/restore were only covered as isolated units.
  - **New `test/features/workout/session_lifecycle_test.dart` (4 journey tests, data-layer end-to-end through `WorkoutRepositoryImpl` with SQLite assertions between stages):** (1) full happy path create → 2 exercise blocks → 4 sets (warm-up + working + planned) → pause with deterministic frozen span → resume → joined re-read → finish with the production duration contract → exact aggregates (working volume 1135 kg excluding warm-up, completedSetsCount 3) → active slot freed for a new session; (2) crash mid-flow — a fresh repository over the same SQLite restores the exact session/sets/elapsed and finishes the inherited journey (L7); (3) multiple pause/resume cycles accumulate `pausedDurationSeconds` (~65–75s) and the stored duration honors the fold; (4) discard path — frozen elapsed time, active slot freed, and the discarded session provably never enters the completed-streak input (`StreakRepositoryImpl` cross-check, §17).
  - **Contract nuance surfaced by the sweep (no code change):** `WorkoutRepositoryImpl.finishWorkout` is a thin pass-through — the epoch-math duration is computed by `ActiveWorkoutController` and handed in as `durationSeconds`; calling it without one leaves the column null (domain getter then falls back to `completedAt − startedAt`). The journey tests mirror the production contract; behavior already covered by `workout_summary_test` fallback assertions.
  - `flutter analyze` (0 issues), `flutter test` (**195/195 passing across mobile** — was 191). No codegen changes (tests only).
  - **NEXT STEP (exact, per user sequencing):** Proceed to **Slice 4 WU-4.1: Nutrition Domain Models & Drift Tables** (`features/nutrition/domain/` — Freezed `FoodItem`, `LoggedMealItem`, `NutritionGoals`, `MealType`; `features/nutrition/data/nutrition_tables.dart` — `FoodItems`, `LoggedMealItems`, `NutritionGoals`; register in `AppDatabase`, schema 7 → 8). Cross-cutting WU-X.1/X.3/X.4/X.5 stay deferred until the user directs them.
- **2026-08-31 (session 29):** Implemented **Slice 4 WU-4.1: Nutrition Domain Models & Drift Tables** (FEATURES.md §11, L10):
  - **Domain (`features/nutrition/domain/`):**
    - `meal_type.dart`: `MealType` enum — the four P0 meals + schema-ready `preWorkout`/`postWorkout` (`[P1][PROPOSED]`, no future migration needed), dashboard labels, and a tolerant `fromName` parse (any casing/`_`/`-`/space style; unknown → `snack` catch-all, never a crash — L6).
    - `food_item.dart`: Freezed `FoodItem` matching the backend V5/V9 shape (macros are **per standard serving** — 40 g roti = 104 kcal) extended with the moat fields: `householdServingUnit` + `householdUnitGramsRatio` (null → falls back to `servingSizeG`), `isVeg`, `isSatvik` (§11.10 set), `foodCategory`, `barcode`. `scaleFor(quantityServings, servingUnit)` is the single macro-math path: `'g'` = grams passthrough, the food's own household unit × ratio, unknown units = standard servings; a zero `servingSizeG` zeroes results so NaN can never reach daily totals (L6). Returns the `FoodItemMacros` value object (grams + kcal/P/C/F/nullable fiber).
    - `logged_meal_item.dart`: Freezed `LoggedMealItem` — day-bucketed (`date` = local midnight) with a **denormalized macro snapshot** taken at log time (day totals stay stable across food-entry edits; mirrors backend `nutrition_entry_items`) + `LoggedMealItem.calculate(...)` factory routing through `FoodItem.scaleFor`.
    - `nutrition_goals.dart`: Freezed `NutritionGoals` singleton ('default' row). **No row = no goals** — the L4 hidden-card state is meaningful absence.
  - **Data (`features/nutrition/data/nutrition_tables.dart`):** `FoodItems` (unique-id catalog incl. custom + barcode), `LoggedMealItems` (FK cascade on food delete — L7; `loggedAt` for intra-meal ordering), `NutritionGoals` (singleton). `app_database.dart`: schemaVersion 7 → 8 with `createTable` × 3 migration. No DAO yet — that is WU-4.3.
  - **Tests (`test/features/nutrition/nutrition_domain_test.dart` — 15 tests):** MealType tolerant parse/labels; FoodItem immutability + JSON round-trip; `scaleFor` exactness (1.5 katori dal = 157.5 kcal / 225 g, gram-linear half-serving, unknown-unit fallback, ratio fallback, NaN guard); `LoggedMealItem.calculate` snapshot math + JSON round-trip; goals round-trip; in-memory Drift CRUD, FK cascade delete (L7), goals singleton insert/update.
  - `dart run build_runner build` (clean), `flutter analyze` (0 issues), `flutter test` (**210/210 passing across mobile** — was 195).
  - **NEXT STEP (exact):** Proceed to **Slice 4 WU-4.2: Bundle Indian Food Database** (`assets/data/indian_foods.json` + `food_seed_loader.dart`). NOTE — EXECUTION_PLAN.md carries **Open Question Q1** on this WU: ship the 201 verified backend entries first and expand iteratively post-MVP (a), invest in curating ~5,000 IFCT-based entries now (b), or a middle ground of ~1,000–2,000 common items (c). **Ask the user for a decision before building the asset.**
- **2026-09-01 (session 30):** Implemented **Slice 4 WU-4.2: Bundle Indian Food Database** (FEATURES.md §11, L10):
  - **User decision (Open Question Q1):** middle ground (~1,000–2,000 common items) with an explicit accuracy mandate. Landed **948 entries** — the 201 backend-verified V9 rows plus 747 curated expansion entries. IFCT is licensed data and not accessible, so expansion values are reference-estimated; reliability is enforced by a hard data-integrity gate instead (below).
  - **Asset (`mobile/assets/data/indian_foods.json`):**
    - The 201 verified entries were extracted from `backend/.../V9__seed_indian_food_items.sql` **by script** (`node` regex row parser) — zero manual transcription. Each maps to the WU-4.1 schema with `householdUnitGramsRatio = servingSizeG` (the standard serving IS one household unit: 40 g roti, 150 g katori dal).
    - Expansion (747) spans 19 categories total: `VEGETABLE`(94), `VEGETABLE_RAW`(36, per-100g standard values), `RAW_STAPLE`(28, raw dals/flours/grains per 100 g), `GRAIN`(85), `DAL_LENTIL`(35), `PANEER_DAIRY`(59), `NON_VEG`(99), `SOUTH_INDIAN`(66), `REGIONAL`(76 — Gujarati/Maharashtrian/Bengali/Rajasthani/Hyderabadi/Goan/Andhra/Kashmiri/Sindhi/Coastal Karnataka/Northeast), `INDIAN_SNACK`(113), `INDIAN_SWEET`(64), `BEVERAGE`(49), `FRUIT`(39), `DRY_FRUIT`(27), `CHINESE_INDO`(22), `VRAT_FASTING`(19), `CONDIMENT_FAT`(18), `SOUP_SALAD`(13), `PROTEIN_SUPPLEMENT`(6).
    - **Satvik flags (§11.10, ships with this WU):** 124 entries flagged — sabudana/kuttu/singhara/samak/makhana/rajgira items, whole fruits, plain dairy (milk/curd/lassi/chaas/plain paneer), nuts & dry fruits, vrat-specific dishes. Cooked dishes with onion/garlic and legumes (Ekadashi rule) stay non-satvik — conservative by design. Satvik ⇒ vegetarian enforced in tests.
    - **Accuracy gates:** every entry validated at build time and asserted per-row in tests: kcal ≈ 4P+4C+9F (Atwater) within max(25 kcal, 20%), positive servings, non-empty household units, zero duplicate IDs/names, `isCustom=false`, `barcode=null`. Expansion UUIDs assigned sequentially from `0x100001` (verified core max is `0x962`) — no collisions.
  - **Loader (`features/nutrition/data/food_seed_loader.dart`):** mirrors `ExerciseSeedLoader` — `seedInitialData(db, {bundle, force})` with a first-run count guard (existing rows → no-op), `seedFromJsonString` for direct seeding, `insertOrReplace` batch (force re-seed is idempotent), and `countFoodItems`. Takes `AppDatabase` directly (no NutritionDao yet — that's WU-4.3). `assets/data/` was already directory-registered in pubspec — no change needed.
  - **Wiring note:** the exercise catalog seeds via `ExerciseListController.build()` → `repository.seedInitialData()`; the nutrition equivalent arrives with the WU-4.3 controller, which should call `FoodSeedLoader.seedInitialData` the same way.
  - **Verification & Tests (`test/features/nutrition/food_seed_test.dart` — 10 tests):** full-catalog insert (DB count == asset count ≥ 900), first-run guard preserves tampered rows, force re-seed restores them, `dal` search <300ms with ≥10 hits, `paneer` search asserting exact household units/ratios (raw 100 g vs katori 200 g), katori calibration sweep on dal entries, veg/non-veg population + satvik⇒veg invariant, §11.10 satvik spot-checks, per-row data-integrity sweep, and domain `scaleFor` macro math on a seeded row (1 katori = 150 g exact, 1.5 katori linear, gram passthrough). Hermetic `AssetBundle` fake covers the rootBundle path.
  - **Note:** the extraction/merge/validator scripts (Node, `extract_core.js`/`merge_foods.js` + 13 batch files) were session-scratchpad tooling and are not in the repo; the durable accuracy gate is the test suite's per-row Atwater invariants, which any future expansion batch must pass.
  - `dart run build_runner build` (clean, no schema change), `flutter analyze` (0 issues), `flutter test` (**220/220 passing across mobile** — was 210).
  - **NEXT STEP (exact):** Proceed to **Slice 4 WU-4.3: Nutrition DAO & Repository** (`features/nutrition/data/nutrition_local_source.dart` — `NutritionDao` with `searchFoods(query, {vegOnly, satvikOnly})` <300ms, `logMealItem`, `watchDailyLog(date)`, `watchDailyTotals(date)`, quantity updates, custom foods, goals read/write; `nutrition_repository.dart` + `@riverpod` provider; wire `FoodSeedLoader.seedInitialData` into the controller path like the exercise catalog does).
- **2026-09-01 (session 31):** Implemented **Slice 4 WU-4.3: Nutrition DAO & Repository** (FEATURES.md §11, Laws L1/L2/L4/L7/L8):
  - **Domain anchors (`features/nutrition/domain/`):**
    - `logged_meal_item.dart`: added `LoggedMealItem.normalizeDay` — the single local-midnight day-anchor that both the write side (log persistence) and the read side (daily queries) bucket through, so a stray time component can never split a day.
    - `nutrition_log_entry.dart` (new): plain carrier `NutritionLogEntry` (item + food) — the dashboard's joined display payload (macro snapshot alongside food name/unit/flags); the UI never sees drift rows.
  - **Data layer (`features/nutrition/data/`):**
    - `nutrition_local_source.dart`: `NutritionDao` (registered in `AppDatabase.daos`; no schema change — `schemaVersion` stays 8):
      - `searchFoods({query, vegOnly, satvikOnly})` — case-insensitive LIKE on name+brand, sub-300ms over the ~950-entry bundled catalog (benchmark-verified); the veg/satvik filters feed the §11.10/L10 surfaces.
      - `watchDailyLog(day)` — reactive inner join of `LoggedMealItems` + `FoodItems` for a local-midnight day, ordered by `loggedAt`; re-emits on log mutations and catalog changes.
      - `watchDailyTotals(day)` — derived from the log stream (summed per emission, never a stored aggregate, so edits/deletes self-heal — L7/L8); `DailyNutritionTotals` carrier (itemCount, kcal/P/C/F, nullable `fiberG`: unknown stays unknown, never silently zeroed).
      - `getLoggedItemWithFood`, `insertMealItem`, `updateMealItemSnapshot` (write-through half of the inline stepper), `deleteMealItem` (confirmation stays a UI concern), `getFoodItemById`, `insertFoodItem` (custom foods; bulk seeding remains in `FoodSeedLoader`).
      - Goals singleton: `getNutritionGoals` / `watchNutritionGoals` (`watchSingleOrNull` — emits null while unset so the dashboard hides the calories-remaining card entirely, L4 meaningful absence) + `upsertNutritionGoals` (insert/update paths mirroring the streak settings singleton; full-replace semantics).
    - `nutrition_repository.dart`: `NutritionRepository` contract + `NutritionRepositoryImpl` + `@riverpod nutritionRepositoryProvider`.
      - `logMealItem` computes the denormalized snapshot through `LoggedMealItem.calculate` → `FoodItem.scaleFor` (the single macro-math path); unknown food id → `StateError`. `updateMealItemQuantity` recomputes through the same path while preserving the log's identity (id/date/meal/unit/notes/loggedAt) — only quantity + macros move (<100ms, L1).
      - `createCustomFood` (`food_custom_*` ids, `isCustom: true`), `deleteMealItem`, domain-mapped `watchDailyLog`, `setNutritionGoals` upsert, `searchFoods`/`getFoodItemById` mapping.
      - `seedInitialData({force})` wires `FoodSeedLoader.seedInitialData` into the repository path (the exercise-catalog pattern; the WU-4.4 search controller should call it in `build()` like `ExerciseListController` does). Impl takes an optional hermetic `seedBundle` test seam (null → rootBundle).
  - **Tests (`test/features/nutrition/nutrition_dao_and_repo_test.dart` — 19 tests):** search ('dal' <300ms with catalog hits, vegOnly excludes non-veg, satvikOnly §11.10 restriction incl. biryani exclusion, by-id exactness); logging (katori exactness 1 katori = 150 g = 105 kcal, gram passthrough, local-midnight bucketing with no cross-day bleed, daily totals == sum of item snapshots incl. unknown-fiber semantics, totals stream reactivity on new logs, preWorkout tolerant round-trip, unknown food id rejection); quantity stepper (recalc via scaleFor preserving log identity, persisted totals reflect the new snapshot, unknown id → null); deletion (row removal + totals shrink, repeat delete → false); custom foods (isCustom row persisted + searchable, own household-unit math 2 bowls = 600 g = 720 kcal); goals (L4 absence → null watch, singleton upsert with full-replace semantics); seed wiring (≥900 entries, first-run guard no-op).
  - `dart run build_runner build` (2 outputs), `flutter analyze` (0 issues), `flutter test` (**239/239 passing across mobile** — was 220).
  - **NEXT STEP (exact):** Proceed to **Slice 4 WU-4.4: Food Search & Item Detail Screens** (`features/nutrition/presentation/` — `food_search_controller.dart` debounced search + veg/satvik filter toggles, `food_search_screen.dart` with FSSAI veg indicator + household serving display ("1 katori · 150g · 180 kcal"), `food_detail_controller.dart` dynamic macro scaling on quantity/unit change, `food_detail_screen.dart` with serving selector (grams OR household dropdown), quantity stepper (0.5/1/1.5/2/custom), meal type selector, "Log" → SQLite → meal view; call `repository.seedInitialData()` in the search controller's `build()` like `ExerciseListController` does).
- **2026-09-01 (session 32):** Implemented **Slice 4 WU-4.4: Food Search & Item Detail Screens** (FEATURES.md §11.3/§11.4, Laws L1/L2/L6/L7/L10):
  - **Search (`food_search_state/controller/screen.dart`):**
    - `FoodSearchController` — real 300ms debounce (`Timer` + `ref.onDispose` cancel; query reflects immediately, results refresh once); seed-on-first-build via `repository.seedInitialData()` (the WU-4.2 contract, mirroring `ExerciseListController`); veg-only / satvik-only toggles (§11.3/L10) and `clearFilters`.
    - `FoodSearchScreen` — search bar, FSSAI **veg mark rendered from data** (volt-green dot veg / burnt-orange triangle non-veg, custom painter — never guessed, L10), household serving line on every tile ("1 katori · 150 g"; gram-unit entries collapse to "N g serving"), SATVIK/CUSTOM badges, kcal badge (`AppTheme.num`), result count chip, and the L6 designed empty state with CLEAR FILTERS.
  - **Detail (`food_detail_state/controller/screen.dart`):**
    - `FoodDetailState` — macros are **never stored**: `macros` recomputes through `FoodItem.scaleFor` on every state change (pure math, <100ms per tap — L1; L8-style derived state). `unitOptions` = household unit + grams (collapses for gram-unit foods).
    - `FoodDetailController` — family-by-foodId; `setQuantity` (clamped positive), `selectUnit`, `selectMeal`, and `log()` → repository write-through returning the stored `LoggedMealItem` (screen pops to the meal view on success; the WU-4.5 dashboard's reactive stream picks it up). Unknown id → null → designed not-found state (L6).
    - `FoodDetailScreen` — header with veg mark/brand, SERVING card (unit dropdown + 0.5/1/1.5/2 chips + CUSTOM numeric dialog, live "≈ N g" resolution), NUTRITION panel (big kcal, P/C/F rows, **fiber "—" placeholder when unknown — never zeros**, L6), ADD TO MEAL chips, sticky `LOG TO <MEAL>` volt-green action with LOGGING… guard.
    - **Riverpod 3 hardening:** `ref.mounted` guards after every async gap (seed, search, log) — a mid-flight dispose can never touch a disposed notifier; the log write-through still lands and the result is handed back. (Riverpod disposes listener-less autoDispose providers immediately — real screens always have a listener, but the guard closes the mid-write race.)
  - **Wiring:** routes `/foods` and `/foods/:id?meal=<hint>` (hint preselects the meal bucket post-frame, e.g. `?meal=lunch` for the WU-4.5 dashboard flow); the Nutrition tab placeholder gained a temporary FOOD DATABASE entry button (replaced by the dashboard in WU-4.5). Custom-food creation UI remains §11.5 `[P1][PROPOSED]` — the repository path (`createCustomFood`) is ready.
  - **Tests (`test/features/nutrition/food_search_test.dart` — 18 tests):** controller (seed-on-build ≥900, debounced filtering + query-immediate/results-after semantics, vegOnly excludes non-veg incl. 'chicken'+veg → empty, satvikOnly §11.10 restriction, clearFilters restores); detail controller (household defaults + unitOptions, not-found null, quantity/unit recompute <100ms with exact math 1.5 katori = 157.5 kcal / 50 g = 35 kcal, meal switch, write-through log → daily log via reactive stream); search screen (render + chips + debounced filter + scroll-to-tile household line, L6 empty state with CLEAR FILTERS escape); detail screen (panel kcal/grams/macros, fiber "—" placeholder on a custom food, quantity + meal chips update instantly, CUSTOM quantity dialog (3 katori = 315 kcal), `?meal=breakfast` preselect, log-to-lunch pops back and lands the item in SQLite verified by a one-shot read).
  - **Test-craft notes (recurring pitfalls):** (1) autoDispose controllers need a keep-alive `container.listen(...)` across async waits in tests — Riverpod 3 disposes listener-less providers mid-test ("Ref used after dispose"); (2) drift **streams** never emit their first value inside the fake-async `testWidgets` zone — assert with one-shot `db.select().get()` there (streams are fine in plain async tests); (3) `ListView.builder` never builds off-screen tiles — use `scrollUntilVisible(..., scrollable: find.byType(Scrollable).last)` (the first Scrollable here is the horizontal chips bar).
  - `dart run build_runner build` (clean), `flutter analyze` (0 issues), `flutter test` (**257/257 passing across mobile** — was 239).
  - **NEXT STEP (exact):** Proceed to **Slice 4 WU-4.5: Nutrition Dashboard Screen** (`features/nutrition/presentation/` — MODIFY `nutrition_screen.dart` into the full dashboard: day navigation (← Today →), calories-remaining card **hidden without goals** (L4), macro progress bars as cyan/grey flat bars — protein emphasized first, never red (L4), 4 meal sections (Breakfast/Lunch/Dinner/Snacks) each with logged items + inline quantity stepper (`updateMealItemQuantity`) + remove (confirm, §11.2) + subtotal + "Add Food" → `/foods?meal=<x>`, daily totals summary at bottom; `nutrition_dashboard_controller.dart` watching `watchDailyLog`/`watchDailyTotals`/`watchNutritionGoals` + selected-day state; `widgets/macro_progress_bar.dart` + `widgets/meal_section_card.dart`).
- **2026-09-01 (session 33):** Implemented **Slice 4 WU-4.5: Nutrition Dashboard Screen** (FEATURES.md §11.1/§11.2, Laws L1/L2/L4/L6/L7/L8):
  - **State & controller (`nutrition_dashboard_state.dart` / `nutrition_dashboard_controller.dart`):**
    - `NutritionDashboardState` — Freezed (`day`, `entries`, `totals?`, `goals?`) with computed getters: `hasCalorieGoal` (goals row with a positive target — otherwise the calories card is hidden entirely, L4 meaningful absence), `consumedKcal`, `caloriesRemaining` (can go negative — a neutral fact, never red), `mealEntries` (grouped in §11.1 section order), `mealKcal(meal)`.
    - `SelectedNutritionDay` — autoDispose Notifier anchoring the selected day through `LoggedMealItem.normalizeDay` (the single day-anchor, WU-4.3); `previousDay`/`nextDay`/`goToToday`/`select`.
    - Three `@riverpod` stream providers (`dailyLogStream`/`dailyTotalsStream` keyed by day, `nutritionGoalsStream`) mirroring the `watchActiveSessionProvider` pattern (L8 — drift re-emits, zero polling); `NutritionDashboardController` is a sync Notifier composing them. Mutations (`updateItemQuantity`, `removeItem`) are thin write-through pass-throughs to the repository — the streams re-emit the new state, so the macro math is never duplicated (single path: `LoggedMealItem.calculate` → `FoodItem.scaleFor`, L1/L7).
  - **Widgets (`presentation/widgets/`):**
    - `macro_progress_bar.dart` — adherence-neutral bar: flat cyan fill on a glassBorder track, zero radius, no gradients (L4); fill clamps at full width on over-target days while the value text keeps the fact; target ≤ 0 renders no bar visual (a percentage of nothing would be a fake verdict, L4/L6); `emphasized` variant for protein (§11.1/§11.9 — taller track, stronger label, rendered first).
    - `meal_section_card.dart` — glass card per meal: label + item count + kcal subtotal; logged rows (name, serving line "1.5 katori · 225 g · 9 g protein" resolved through `scaleFor` with the item's logged unit, inline ±0.5 steppers keyed `step_up/down_<id>`, kcal, remove keyed `remove_item_<id>`); designed "Nothing logged yet." empty state (L6); ADD FOOD keyed `add_food_<meal>`.
  - **Screen (`nutrition_screen.dart` rebuilt from the placeholder):** header, day navigation (← TODAY → with the future unreachable — forward disabled at today), calories-remaining card (hidden without goals — L4; over-target shows "CALORIES OVER TARGET" + |delta| in grey — never red), four meal sections ("Add Food" → `/foods?meal=<bucket>`), DAILY TOTALS summary (fiber renders "—" when unknown, L6). Remove requires confirmation (§11.2/L7).
  - **Wiring:** `/foods` route + `FoodSearchScreen` gained an optional `mealHint` (`?meal=<x>`) forwarded to `/foods/:id?meal=` so the dashboard's Add Food flow lands the item in the originating meal bucket; the `/nutrition` tab now renders the full dashboard.
  - **Test-craft discovery (fixes a gap in the WU-3.10/WU-4.4 fake-async notes):** with an active drift stream subscription, `db.close()` in tearDown **hangs** the test — close awaits a stream-cleanup timer that can never fire once the fake-async body has ended (even when the tree was unmounted without a container dispose). The deterministic drain: **unmount (`pumpWidget(SizedBox.shrink())`) → `container.dispose()` → `pump`** inside the body (encoded in `pumpDashboard`'s `addTearDown`); `db.close()` then completes cleanly. Related: the entries and totals streams emit independently — poll for a **consistent snapshot** (both conditions), never entries alone, or an intermediate rebuild pairs new entries with stale totals.
  - **Verification & Tests (`test/features/nutrition/nutrition_dashboard_test.dart` — 15 tests):** controller (cross-meal totals == sum of the denormalized snapshots + §11.1 bucketing, L4 goals absence → flip on set + remaining math, over-target −5, day-nav re-subscription with date-scoped logs, <100ms inline stepper with totals self-heal, remove self-heal); screen (sections/items/serving line/subtotals/DAILY TOTALS render, L4 hidden card, goals card + emphasized protein bar with exact consumed/target text, over-target neutral fact + no bar visual without targets, day-nav label/data switch, stepper write-through verified with a one-shot SQLite read, remove confirm + cancel keeps item, Add Food meal-hint navigation).
  - `dart run build_runner build` (184 outputs), `flutter analyze` (0 issues), `flutter test` (**272/272 passing across mobile** — was 257).
- **2026-09-02 (session 34):** Implemented **Slice 4 WU-4.6: Slice 4 Tests** — **Slice 4 is now ✅ COMPLETE**:
  - **Coverage audit vs the WU-4.6 checklist:** `nutrition_domain_test.dart` ✓ (15 tests — Freezed models, scaleFor macro math), `food_search_test.dart` ✓ (18 tests — search accuracy, veg/satvik filters, household units), and meal-logging → daily-totals parity plus goals set/unset + remaining are covered across `nutrition_dao_and_repo_test.dart` (19 tests) and `nutrition_dashboard_test.dart` (15 tests). No separate `nutrition_goals_test.dart` formalized — the repository has no unset-goals API and absence/set/replace + remaining math are already pinned.
  - **Real gaps found (the WU-3.11 audit precedent):** no connected **J3 journey** (the AGENTS.md Quality Matrix merge gate for nutrition slices), the **mixed fiber semantics** unpinned (the dao test's totals-fiber assertion was fixture-conditional), and the **fresh-install empty-catalog state** (0-row search + zeroed totals before first seed) untested.
  - **New `test/features/nutrition/meal_logging_test.dart` (3 journey tests, data-layer end-to-end through `NutritionRepositoryImpl` over in-memory SQLite with persisted-state assertions between stages):** (1) J3 happy path — fresh install (empty search, zeroed totals, L4 goals absence) → first-launch seed via the production repository path (≥900 entries) → 'dal' search <300ms → 1 katori logged to Lunch <100ms with the exact snapshot → multi-meal day (roti ×2 breakfast, banana snack, custom 'bowl' food dinner through its own household unit) → daily totals == sum of the denormalized snapshots incl. fiber parity (known snapshots only) → inline stepper identity preservation + totals self-heal → removal shrink → goals flip with the day's food data undisturbed; (2) fiber semantics — unknown-only day → `totals.fiberG` null (never a silently-zeroed 0, L6 "—"), mixed day → sum of the known snapshots only; (3) day isolation — steppers and removals on one day never bleed into the other's totals, goals are day-agnostic.
  - **Journey-surface contract nuance (no code change):** custom foods scale through their own household-unit ratio — a 100 g-serving/250 g-bowl entry at 1 bowl = 2.5 standard servings = 2.5 × macros (mirrors the `scaleFor` contract; the suite encodes the correct math).
  - No schema/lib changes (tests only). `dart run build_runner build` (clean), `flutter analyze` (0 issues), `flutter test` (**275/275 passing across mobile** — was 272).
  - **NEXT STEP (exact, per user sequencing):** Proceed to **Slice 5 WU-5.1: Auth Domain & Secure Storage** (`features/auth/domain/auth_state.dart` — Freezed `Guest(clientUuid)` / `Authenticated(userId, accessToken, refreshToken)` / `Loading` / `Error`; `features/auth/data/auth_local_source.dart` — `flutter_secure_storage` token + guest-UUID persistence; `auth_repository.dart` — `watchAuthState`/`loginWithOtp`/`loginWithGoogle`/`logout`/`isGuest`; **MODIFY** `core/network/api_client.dart` — Bearer-token interceptor with 401 → refresh → retry). Cross-cutting WU-X.1/X.3/X.4/X.5 stay deferred until the user directs them.
- **2026-09-02 (session 35):** Implemented **Slice 5 WU-5.1: Auth Domain & Secure Storage** (FEATURES.md §6, Laws L2/L6/L7). A session terminated mid-WU was recovered: the audit showed only `pubspec.yaml` (+`flutter_secure_storage ^9.2.4`) and the domain model had landed; everything below was built and verified this session.
  - **Domain (`features/auth/domain/auth_state.dart`):** Freezed **sealed** union `AuthState` — `loading` (brief secure-storage boot read) / `guest(clientUuid)` (the L2 primary state: fresh installs boot straight to Guest, no login wall) / `authenticated(userId, accessToken, refreshToken, displayName?, phoneNumber?)` / `error(message)`. Exhaustive-switch verified in tests.
  - **Secure storage (`features/auth/data/auth_local_source.dart`):** `AuthLocalSource` over `flutter_secure_storage` (Android Keystore-backed) — `readSession` (a partial token pair reads as signed out), `writeSession`, `clearSession` (token pair + profile fields only — the **guest UUID deliberately survives** so sign-out returns to the same anonymous identity, §6.2/L7), `readOrCreateGuestUuid` (created once, never regenerated), and a dependency-free RFC 4122 **UUIDv4** generator (client UUIDs are the J6 sync-contract identity unit). Platform failures throw; the repository decides the fail-safe.
  - **Core network (`core/network/api_client.dart` MODIFY):** added `apiClientProvider` (manual Provider, mirrors `appDatabaseProvider`) and `AuthInterceptor` — Bearer attach (skipped for `avenfit_skip_auth`-flagged auth-engine calls), and on a 401: one-shot refresh + retry with the new token (a `avenfit_auth_retried` extra flag makes a second 401 impossible to loop); `/api/auth/*` 401s surface directly (they are answers, not triggers). Core stays feature-agnostic — the auth repository binds its own closures. Two `prefer_initializing_formals` lint infos fixed with public initializing formals (API unchanged).
  - **Repository (`features/auth/data/auth_repository.dart`):** `AuthRepository` contract + impl + keepAlive `authRepositoryProvider` (over `apiClientProvider`). Boot load-once guard: stored session → Authenticated, else Guest (a broken secure store falls back to Guest **without** touching stored tokens — L2). `requestOtp` (returns the backend's 300s expiry for the resend timer), `loginWithOtp`/`loginWithGoogle` (unwraps the backend `{data, message}` `ApiResponse` envelope, persists the pair, emits Authenticated), `logout` (best-effort `/api/auth/logout` revocation — network failure never blocks local sign-out, L2), `isGuest`, `watchAuthState` (replays current state then follows transitions). `AuthException` carries the backend message + status (screens surface it directly, §6.1/L6). `_refreshAccessToken` (interceptor callback): definitive 401/403 from the refresh endpoint clears the session → Guest (never locked out of offline data); a transient/offline failure **keeps** the session — no wrongful logout.
  - **Bug found & fixed by the journey test (root cause recorded):** the boot guard `_ensureLoaded()` originally called the public `continueAsGuest()`, which awaits `_ensureLoaded()` — a **circular await** deadlocking every boot path (surfaced as 30s test timeouts). Fixed by splitting `_enterGuestMode()` (no guard inside) used by both the loader and the public method, with the why documented inline.
  - **Backend contract alignment:** `POST /api/auth/phone/request-otp` / `verify-otp` / `google` / `refresh` / `logout` (Bearer required for logout only), `OtpRequest{phoneNumber, otp}`, `AuthResponse{accessToken, refreshToken, user{id, phoneNumber, displayName, unitPreference}}`; leading-`/` Dio paths resolve against the origin, correctly bypassing the client's `/api/v1` base.
  - **Verification & Tests (`test/features/auth/auth_test.dart` — 22 tests):** domain union exhaustiveness; local source (uuid format/stability/survives clear, session round-trip, partial-pair semantics); repository (fresh boot → Guest with persisted uuid + zero network, stored-session boot, stream replay + transitions, requestOtp body/expiry, login success paths, invalid-OTP surfaces message and changes nothing, full **Guest → Authenticated → Logout → Guest** lifecycle with stable uuid, offline logout); interceptor (bearer attach, guest bare request, skip-flagged calls, **401 → refresh → one-shot retry** with the rotated pair persisted and the state re-emitted, dead refresh token → signed out, offline refresh → session kept, auth-path 401 no-loop, tokenless 401 no refresh); provider wiring. Harness: method-channel mock for the verified 9.2.4 secure-storage contract + a scripted `HttpClientAdapter` recording **immutable request snapshots** (the interceptor mutates `RequestOptions` in place for retries — a live reference rewrites recorded history).
  - `dart run build_runner build` (409 outputs — auth freezed/riverpod codegen), `flutter analyze` (0 issues), `flutter test` (**297/297 passing across mobile** — was 275).
  - **NEXT STEP (exact, per user sequencing):** Proceed to **Slice 5 WU-5.2: Auth Screens (OTP + Google)** (`features/auth/presentation/auth_controller.dart` managing the login flow state; `login_screen.dart` — Glassmorphism, +91-prefixed phone input with 10-digit inline validation, Send OTP, Google button, prominent "Continue as guest" (L2); `otp_verification_screen.dart` — 6-cell auto-advance input with auto-submit-once, resend timer fed by the repository's `requestOtp` expiry (30s/300s), change-number link, designed verifying/invalid/expired states (L6); wire routes into `app_router.dart` — login reachable from Profile only, never a gate; SMS Retriever autofill stays a P0 stretch — manual entry is the guaranteed fallback). Cross-cutting WU-X.1/X.3/X.4/X.5 stay deferred until the user directs them.
- **2026-09-02 (session 36):** Implemented **Slice 5 WU-5.2: Auth Screens (OTP + Google)** (FEATURES.md §6.1/§6.2, Laws L2/L4/L6/L8):
  - **Login flow (`login_state.dart` + `login_controller.dart`):** Freezed `LoginState` (countryCode, digits-only phone capped at the backend's 15, inlineError, sendingOtp, sendError, infoMessage) with the pure `validatePhone` — 10 digits for +91 (primary), 10–15 for other codes (NRI) mirroring the backend's `^\+?[0-9]{10,15}$`. `LoginController` (autoDispose): `setPhone` digit normalization (errors are per-submit — editing clears), `sendOtp()` validates first (invalid never navigates, L6) → `repository.requestOtp` → returns `({phoneNumber, expiresInSeconds})` for the OTP route; `AuthException` surfaces the backend message into the retry banner; `continueAsGuest()` (L2); **Google deferral** — the repo has no Google Cloud OAuth client/SHA-1 configured (verified: no `client_id` anywhere in `android/`), so `signInWithGoogle()` surfaces a designed notice (`kGoogleSignInSetupMessage`) instead of wiring an SDK that would fail at runtime without credentials; the `loginWithGoogle(idToken)` repository path from WU-5.1 is ready when they land.
  - **OTP flow (`otp_flow_state.dart` + `otp_verification_controller.dart`):** Freezed `OtpFlowState` with epoch deadlines (`resendAtEpochMs`, `expiresAtEpochMs`) + a tick-driven `nowEpochMs` — every displayed value recomputed from deadlines (L8); pure derived getters (`canResend`, `resendInSeconds`, `expiresDisplay` "4:59"). Controller: `otpChanged` (digits, cap 6, error reset), `verify()` with an in-flight guard (**auto-submit fires exactly once per filled code**, §6.2) mapping rejections past the validity window to `expired` (judged against **fresh clock time**, not the stale tick) and others to `invalid` with the cells cleared; `resend()` guarded by fresh clock + 30s cooldown (`kOtpResendCooldownSeconds`), resetting both deadlines; **injectable clock** (`DateTime Function() clock = DateTime.now`, the rest-timer pattern) for deterministic tests.
  - **Screens:** `login_screen.dart` — Glassmorphism wordmark, country-code dropdown (+91 default, NRI codes), phone field (`AppTheme.num` tabular digits), SEND OTP primary, inline burntOrange validation, network-error banner with RETRY, Google outlined button, **prominent volt-green CONTINUE AS GUEST** (L2), DPDP footer; navigates `push('/auth/otp?phone=…&expires=…')`. `otp_verification_screen.dart` — number display, Change number (pop), 6-cell input, designed verifying/invalid/expired states (L6), cooldown-enforced resend, neutral expiry line (L4), VERIFY disabled until 6 digits; success → `context.go('/home')` (§6.2). `widgets/otp_input_field.dart` — one hidden TextField (paste/backspace/auto-advance fall out of a single source of truth) projecting into 6 cells, fill-transition auto-submit tracking, shake (TweenSequence) + medium haptic on invalid.
  - **Routing (`app_router.dart`):** `/auth/login` and `/auth/otp?phone=&expires=` registered — reachable only by explicit navigation (Profile in WU-5.3), never a gate (L2); a deep link without a phone falls back to the login screen.
  - **Test-harness discovery (root-caused via throwaway probe, recorded):** flutter_test's pending-timer invariant fires **before any addTearDown callback runs** — the WU-4.5-style teardown drain cannot cancel periodic timers. Fix: an `authScreenTest` wrapper that drains (unmount → `container.dispose()` → pump) in a `finally` **inside the test body**; probe file deleted after the root cause was pinned. Also: group-scoped fake-clock variables leak across tests (the 'expired' test's +301s broke the blocked-resend test) — per-test `fakeNow = DateTime.now()` anchors keep the injected clock and the build-time deadlines in one coherent frame.
  - **Verification & Tests (`test/features/auth/auth_screens_test.dart` — 29 tests over a hand-written recording `AuthRepository` fake):** login controller (digit normalization/cap, inline errors never calling out, full-phone + backend-expiry return, NRI codes, backend failure → retry state, Google notice, guest delegation); OTP controller (boot deadlines, input filtering, incomplete no-op, success path, invalid state with cleared cells, **exactly-once auto-submit via a parked Completer**, expired-vs-invalid via clock advance, cooldown block/unblock with deadline refresh, failed resend); login screen (render, invalid never navigates, valid → OTP route with query args, guest → Home, Google notice, error banner + RETRY); OTP screen (render + disabled VERIFY, **auto-submit exactly once → Home**, invalid error + cleared cells, change-number pop, resend cooldown unlock with the login-flow's requestOtp baseline accounted for). Codegen (auth presentation freezed/riverpod), `flutter analyze` (0 issues), `flutter test` (**326/326 passing across mobile** — was 297).
  - **NEXT STEP (exact, per user sequencing):** Proceed to **Slice 5 WU-5.3: Profile Screen** (`features/profile/presentation/profile_screen.dart` — Glassmorphism: avatar/name or "Guest" with upgrade banner, lifetime stats (total workouts/volume/sets) aggregated from workout history, quick links (Settings/data-export placeholders, About), Sign In → `/auth/login` when guest, Sign Out with confirmation (L7) when authenticated; `profile_controller.dart` — aggregates auth state (`watchAuthState`) + lifetime stats; wire the Home header avatar → Profile per §7.1 and add the `/profile` route). Cross-cutting WU-X.1/X.3/X.4/X.5 stay deferred until the user directs them.
- **2026-09-02 (session 37):** Implemented **Slice 5 WU-5.3: Profile Screen** (FEATURES.md §12.1/§5.4, Laws L2/L4/L6/L7/L8) — **all mobile Slice-5 WUs are now complete**:
  - **Lifetime stats data layer (`features/history/`):**
    - `domain/lifetime_stats.dart`: Freezed `LifetimeStats` (`workoutCount`, `completedSetCount`, `totalVolumeKg`, `firstSessionAt`) with `volumeDisplay` (trailing-`.0` trim, mirrors `WorkoutHistoryItem`) and `memberSinceDisplay` ("Aug 2026", "—" before the first session — never a fake zero, L6). Derived state — recomputed on every read, never persisted (L7/L8).
    - `history_local_source.dart` `HistoryDao.watchLifetimeStats()`: the streak-DAO dual-trigger pattern — a cheap `customSelect` trigger with `readsFrom: {workoutSessions, workoutSets}` re-executes on either table, and every emission recomputes the totals fresh via typed selects. Semantics mirror the history feed exactly: confirmed sets only, warm-ups excluded from working volume (L1). "Member since" = earliest non-discarded session start (an in-progress first workout already counts; a discarded accidental start never becomes the anchor).
    - `history_repository.dart`: contract + pass-through + reactive `watchLifetimeStatsProvider`.
  - **Auth stream provider (`features/auth/data/auth_repository.dart`):** keepAlive `watchAuthStateProvider` — replays the boot state then follows transitions (Profile re-renders live on sign-in/out, L8).
  - **Profile presentation (`features/profile/` — new feature tree):**
    - `profile_state.dart` / `profile_controller.dart`: `ProfileController` sync Notifier composing the auth union + lifetime stats (`.value ??` placeholders per the nutrition-dashboard pattern); `AuthError` renders like Guest so a broken auth store never blocks the offline core (L2). `signOut()` is a thin write-through pass-through to the repository.
    - `profile_screen.dart`: identity card (avatar circle with the display-name initial, name/phone/Athlete fallbacks — never a blank identity, L6), §5.4 cyan "LOCAL PROFILE — sign in to back up" banner with one-tap SIGN IN (never blocks any flow, L4/L6), 2×2 lifetime stats grid (`WORKOUTS · WORKING VOLUME (KG) · SETS LOGGED · MEMBER SINCE`, `AppTheme.num`), quick links (SETTINGS / DATA EXPORT → designed neutral "coming in a future update" notices, ABOUT → static dialog), and the authenticated SIGN OUT row with a confirmation dialog stating local history survives sign-out and the guest UUID links it back (L7/§5.4).
  - **Wiring:** `/profile` route registered; `HomePage` restructured into the §7.1 header — wordmark + `home_profile_avatar` button (Lucide `circleUserRound`) → Profile; Profile is reached from the Home header, never a tab.
  - **Verification & Tests (`test/features/profile/profile_test.dart` — 14 tests):** domain display helpers; DAO/repository aggregates over in-memory SQLite (empty history → zeros + "—", exact warm-up/planned/discarded exclusions with member-since anchor 1260 kg case, dual-trigger re-emission + un-confirm self-heal); controller composition (guest + authenticated, sign-out delegates to the fake and re-emits Guest through the auth stream); screen (guest render with banner/sign-in/stats/no sign-out, authenticated render with initial avatar + no banner, SIGN IN → login stub, confirm-dialog cancel keeps the session, confirm → repository call + live flip to guest, placeholder notices + About dialog, **Home header avatar → Profile navigation**). Harness notes: the profile overrides must include `authRepositoryProvider` itself — without it `signOut()` falls through to the real repo's secure-storage channel (MissingPluginException), and the autoDispose controller still needs the keep-alive `container.listen` (WU-5.2 lesson, both now encoded in the test file).
  - `dart run build_runner build` (clean), `flutter analyze` (0 issues), `flutter test` (**340/340 passing across mobile** — was 326).
  - **NEXT STEP (exact, per user sequencing):** All planned slice WUs (Slices 1–5) are complete — the only remaining P0 work is the **deferred cross-cutting sweep**, all of which gates the MVP per the dependency graph: **WU-X.1 Home Dashboard Rebuild**, **WU-X.3 Progress Dashboard (MVP)**, **WU-X.4 Empty/Loading/Error states (L6 sweep)**, **WU-X.5 Drift schema migration & codegen sweep**. Awaiting the user's go-ahead on which to start (natural order per the plan: X.1 → X.3 → X.4 → X.5).
- **2026-09-02 (session 38):** Implemented **WU-X.1: Home Dashboard Rebuild** (FEATURES.md §7.1/§5.1, Laws L1/L2/L4/L6/L7/L8). User go-ahead received for the remaining P0 sweep, one WU at a time:
  - **Week-over-week glance data (`features/history/`):** Freezed `WeekStats` (`workoutCount`/`completedSetCount`/`volumeKg` + `volumeDisplay`) and the `WeeklyGlance` record (`thisWeek`/`lastWeek`); `HistoryDao.watchWeeklyGlance()` reuses the dual-trigger `customSelect` pattern, bucketing by the **effective completion date** (`completedAt ?? startedAt` — the same date the streak counts) through the single `StreakInfo.startOfWeek` anchor. **Bug caught by its own test:** a shared `weekEnd` bound made last week's bucket swallow this week's sessions — fixed with explicit per-bucket bounds (`[thisWeekStart, +7d)` / `[lastWeekStart, thisWeekStart)`). Repository pass-through added.
  - **Suggested routine source (`features/routine/`):** `RoutineDao.watchSuggestedRoutineId()` — the §7.1 P0 heuristic (the routine started in the newest non-discarded session, falling back to the newest-created routine; `WorkoutSessions` added to the DAO's accessor tables for the tracked query) + repository pass-through.
  - **Home state/controller (`features/workout/presentation/`):** `HomeState` (Freezed) with pure statics — `greetingLabel` (§7.1 four bands), `dateLabel`, `pickSuggestedRoutine` (last-used → catalog fallback), `caloriesRemaining` (null without goals — the chip hides entirely, L4; negative stays a neutral fact, never red), `volumeDeltaPercent` (null without a baseline — a percentage of nothing is a fake verdict, L6) + `volumeIsNewThisWeek` ("▲ NEW"). `HomeController` (sync Notifier, the nutrition-dashboard pattern) composes **eight** reactive streams: active session, streak, weekly glance, recent 10 workouts, routines + suggested id, nutrition goals, today's totals — all re-emit from SQLite, zero polling (L2/L8).
  - **`home_screen.dart` rebuilt per §7.1 (top → bottom):** greeting + date + avatar header (Profile entry preserved), the §7.1 cyan resume banner (unchanged keys/behavior), the giant primary CTA — **"START FIRST WORKOUT" on first run (§5.1's 60-second goal), "START NEW SESSION" afterwards** — the suggested-routine card (one-tap start through `resolveOneSessionRule` → `RoutineListController.startWorkoutFromRoutine`, the exact tested <1s write-through path; **`RoutineListController` is now `keepAlive`** so an autoDispose teardown can never race the mid-flight write-through), glance chips (THIS WEEK "X of Y" cyan-when-met · VOLUME + ▲x%/▼y% volt/burnt badge or ▲ NEW · SETS · STREAK omitted at 0, L4 · CALORIES LEFT only with goals), RECENT WORKOUTS (last 10, relative dates, `sets · volume · duration`, PR-count chip, tap → detail, VIEW ALL → history) with the designed first-run state ("YOUR HISTORY WILL APPEAR HERE" + the §5.1 "No account needed. Works offline." explainer, L6).
  - **Test-harness lesson (root-caused, now the default):** the rebuilt Home watches the same stream mix as the nutrition dashboard, so `db.close()` in tearDown **hangs** any Home-backed widget harness that relied on the old "undisposed container" pattern (streak_test + session_restore_test Home groups hung the full suite). Fix everywhere: the **WU-4.5 drain** (`addTearDown`: unmount → `container.dispose()` → `pump(100ms)`) — applied to `streak_test`, `session_restore_test`, and the new `home_dashboard_test`; `widget_test` needs none (it never closes its DB).
  - **Tests (`test/features/workout/home_dashboard_test.dart` — 17):** statics (greeting bands, date, suggestion pick, L4/L6 calories + delta semantics), DAO weekly-glance (empty zeros, effective-date bucketing with warm-up/planned exclusions and cross-week completedAt), controller composition + `startSuggestedRoutine` write-through (session carries routineId/name, routine unmutated), screen (first-run states, with-history cards/relative dates/VIEW ALL, card tap → detail, suggested one-tap start, calories hidden/shown, ▲ 10% badge). Updated for the rebuild: `widget_test` + `session_restore_test` expect the first-run CTA label on a fresh DB; `profile_test`'s Home test now overrides `appDatabaseProvider` (hermetic) since Home resolves all streams through it.
  - `dart run build_runner build` (clean), `flutter analyze` (0 issues), `flutter test` (**357/357 passing across mobile** — was 340).
  - **NEXT STEP (exact):** Proceed to **WU-X.3: Progress Dashboard (MVP)** — MODIFY `features/history/presentation/progress_screen.dart` into the dashboard (PR Vault preview: top recent PRs with exercise/value/date, View-All → the new `features/progress/presentation/pr_vault_screen.dart` grouped by exercise with all 4 PR types, streak display, recent 5 workouts → full history, body-weight placeholder card for P1), then **WU-X.4 (L6 states sweep)** and **WU-X.5 (schema/codegen sweep)**.
- **2026-09-02 (session 39):** Implemented **WU-X.3: Progress Dashboard (MVP)** (FEATURES.md §10/§17, Laws L2/L4/L6/L8):
  - **Data (`features/progress/`):** `PrDao.watchVault()` — reactive joined stream (personal_records ⟕ exercises), newest `achievedAt` first, returning the `PRVaultEntry` carrier (drift row + exercise name; the outer join is belt-and-braces since FK cascade already orphans nothing, L7); `PRRepository.watchVault()` pass-through; `prVaultStreamProvider` in the new `features/progress/presentation/pr_vault_controller.dart` — one stream shared by the Progress preview and the full vault (L8). Domain addition: `RecordType.formatValue(value, {weightKg})` — the single per-type display formatter ("100 kg" / "117.5 kg e1RM" / "8 reps @ 60 kg" / "360 kg", trailing-`.0` trim) so preview and vault can never diverge.
  - **Progress dashboard (`features/history/presentation/progress_screen.dart` rebuilt from the placeholder):** streak card (§17 — "X of Y" forgiving progress, cyan when met, with the current chain shown as a neutral "0 weeks" fact, never a shaming verdict, L4), **PR Vault preview** (newest 5 records: exercise name, volt-green value, type badge, relative date, tap → vault) + VIEW ALL → `/progress/prs`, **Recent workouts** (top 5 compact cards mirroring Home's, PR-count chips, tap → `/history/:id`) + VIEW ALL → `/history`, and the **Body Weight placeholder** (§10.3 [PROPOSED] — designed P1 card, L6). Empty states designed for both sections (L6); VIEW ALL actions only render when there is something to view.
  - **PR Vault screen (`features/progress/presentation/pr_vault_screen.dart`):** full vault grouped by exercise (first-seen order = each group's newest achievement; newest-first within), every record row showing its type badge/value/date via the shared `formatValue`; loading/error/designed empty states (L6).
  - **Routing:** `/progress/prs` registered.
  - **Tests (`test/features/progress/progress_dashboard_test.dart` — 10):** formatter exactness (all four types, trailing-`.0` trim, weight-key default); DAO vault (empty, joined names, newest-first order, weight-key preserved, reactive re-emission on insert); screens over real in-memory SQLite with the WU-4.5 drain — fresh-DB zero facts + designed empties, with-data streak ("3 of 3" / "1 week" via the streak_test seeding pattern), PR preview + recent card + both VIEW ALL navigations, vault grouping with all three record types, vault empty state. Contract note: the drift row's `recordType` is text — parsed through the tolerant `RecordType.fromName` at the UI edge.
  - `dart run build_runner build` (clean), `flutter analyze` (0 issues), `flutter test` (**367/367 passing across mobile** — was 357).
  - **NEXT STEP (exact):** Proceed to **WU-X.4: Empty/Loading/Error states (L6 sweep)** — audit every screen's loading/error/empty rendering against L6 and fill the gaps (error+retry paths are the most common miss), then **WU-X.5 (Drift schema migration & codegen sweep)**.
- **2026-09-02 (session 40):** Implemented **WU-X.4: Empty/Loading/Error States (L6 sweep)** (Law L6, FEATURES.md §4):
  - **Canonical state widgets (`core/widgets/`):**
    - `empty_state_widget.dart` — `EmptyStateWidget`: glass card with icon + title + optional explainer + optional action button (adherence-neutral copy, L4).
    - `loading_state_widget.dart` — `LoadingStateWidget`: glass **skeleton** loader (header bar + rows). Deliberately static: local reads finish in <2s cold start (L2), so an animated shimmer would burn frames/battery on the ₹9,000 target for nothing (L5/L8 spirit).
    - `error_state_widget.dart` — `ErrorStateWidget`: burnt-orange alert + message + keyed RETRY button (`error_state_retry`) that re-executes the failed read via `ref.invalidate(provider)` — an error is never a dead end (L6).
  - **Error-recovery sweep (the audit's real gap):** seven screens had error branches with no recovery path — **routine list/editor/detail, exercise detail, food detail, create-custom-exercise, PR vault** now render `ErrorStateWidget` and recover through `ref.invalidate` of their own provider (family providers invalidated with their argument). Screens that already satisfied L6 (history list, workout summary, workout detail, active workout, exercise picker, exercise list, food search — each already had RETRY, either bespoke or `ref.refresh`) were left untouched. Superseded private error/empty widgets in `pr_vault_screen.dart` deleted completely (L7 hygiene).
  - **Loading sweep:** all thirteen full-body loading branches now render the glass skeleton instead of a bare spinner (exercise list/detail, create-custom-exercise, routine list/detail/editor, exercise picker, active workout, workout summary, workout detail, history list, food search, food detail, PR vault). No test asserted spinners, so this is behavior-neutral.
  - **Audit result:** every list surface has a designed empty state (workouts, routines, exercises + search-no-results, foods + per-meal sections, history, PR vault, home/progress sections); every async load renders a state; every error now recovers; no blank screens (L6). The Progress tab's stream sections collapse individually on error while the page frame stays — the vault (full-screen stream) got the retry.
  - `flutter analyze` (0 issues), `flutter test` (**367/367 passing across mobile** — no count change: behavior-neutral sweep). No codegen changes.
  - **NEXT STEP (exact):** Proceed to **WU-X.5: Drift Schema Migration & Codegen Sweep** — consolidate table registrations in `app_database.dart`, verify the migration strategy v1→v8 is complete and linear (each version step exists), run a final clean `build_runner` pass, and verify `flutter analyze` 0 issues + full `flutter test` green.
- **2026-09-02 (session 41):** Implemented **WU-X.5: Drift Schema Migration & Codegen Sweep** (core/database) — **the P0 mobile plan is now 100% complete (Slices 1–5 + WU-X.1–X.5)**:
  - **Audit result — already satisfied, no changes needed:**
    - All **14 tables** (WorkoutSessions, SessionExercises, WorkoutSets, MuscleGroups, Exercises, ExerciseMuscleGroups, Routines, RoutineExercises, RoutineSets, PersonalRecords, StreakSettings, FoodItems, LoggedMealItems, NutritionGoals) and all **7 DAOs** are consolidated in the single `@DriftDatabase` registration.
    - The migration chain is complete and linear through `schemaVersion 8`: v2 exercise tables → v3 routine tables → v4 session_exercises → v5 personal_records → v6 pause columns (`addColumn` ×3) → v7 streak_settings → v8 nutrition tables ×3. The `from < N` guard pattern converges every upgrade path to the latest schema; `PRAGMA foreign_keys = ON` runs in `beforeOpen`.
  - **From-scratch codegen verification (the sweep's substance):** all **84 tracked generated files** (`.g.dart`/`.freezed.dart` across `lib/` + `test/`) were deleted and regenerated with a single `build_runner` pass — `git status` confirms every output reproduced **byte-identical** with zero stray or missing files. Codegen is fully self-consistent from sources.
  - **Final gates:** `flutter analyze` (0 issues), `flutter test` (**367/367 passing across mobile**) on the freshly generated tree.
  - **NEXT STEP (user's call):** All planned P0 WUs are complete — the P0 MVP mobile scope per `EXECUTION_PLAN.md` (dependency graph: S1–S5 ✅, Home Dashboard ✅, Profile ✅, L6 states ✅, Foreground Service ✅) is done. Remaining work is Post-MVP (`P1/V1.1/V2`: CSV importer, EMA TDEE, Vrat Mode, sync hardening, AI surfaces) and device-level verification (J1/J2 journey runs on an emulator). Recommend `git push` of the 24-commit `dev` ahead of `origin/dev` when ready.
- **2026-09-02 (session 42):** **Full P0 MVP review + test battery** (FEATURES.md end-to-end as source of truth):
  - **Battery results — all green:** `flutter analyze` 0 issues; `flutter test` **367/367 → 368/368** (one new regression test, below); backend `./gradlew test` **BUILD SUCCESSFUL** (exit 0). No test failures anywhere.
  - **Review verdict:** every P0-tagged feature in FEATURES.md (§5 onboarding, §6 auth, §7 home, §8 workout core loop incl. ghost prefill/rest timer/summary/history/detail/routine editor, §9 exercises, §10 progress + PR vault, §11 nutrition, §12.1 profile, §16.1 foreground service, §17 streak) is implemented and tested. The audit surfaced ONE genuine error-path defect, now fixed:
    - **§8.1 save-error state (fixed):** `finishWorkout()` used `AsyncValue.guard`, so a failed finish write-through collapsed the entire logging surface into the full-screen load-error whose RETRY offered to *start a new workout* — the exact anti-pattern §8.1's "save error with retry (data stays local, always recoverable)" forbids. Fix: the finish keeps the view fully alive (`isSaving` flag; double-finish guarded), sets `errorMessage`, and the screen shows a designed floating snackbar — "Could not save workout — your sets are safe on this device." — with a RETRY action re-running the finish. Regression test in `session_restore_test.dart` ('Finish save-error path'): a mocked failing write-through leaves session + both sets in state and the row active in SQLite; the retry (stub forwarded to the real repo) completes the workout for real.
  - **Deferred/blocked P0 ledger (not defects — recorded for the user):**
    - **Google Sign-In wiring** (§6.1 core P0): the Dart-side `loginWithGoogle(idToken)` path is ready and the button renders, but the actual SDK needs a Google Cloud OAuth client + SHA-1 registration (verified absent in `android/`) — runtime would fail with DEVELOPER_ERROR until the user provisions credentials.
    - **SMS Retriever OTP autofill** (P0 stretch, WU-5.2): manual 6-digit entry is the guaranteed fallback and fully tested.
    - **Unapproved `[P0][PROPOSED]` proposals** (need explicit approval per the promotion rule, ledger #s): setup quiz (#1), starter routine templates (#2), copy-previous-set (#4), undo snackbar (#5), keep-screen-awake (#7), workout calendar heatmap (#9), lock-screen 1-tap logger (#41).
    - **Minor spec deviations:** auto-naming concatenates both band names instead of picking one randomly (§8.1 table); PR vault lacks filter/sort controls (§10.2 untagged line); in-session exercise block header has no quick-info sheet (§8.1); strings are not externalized to l10n resources (§15 "localization-ready").
  - `flutter analyze` (0 issues), `flutter test` (**368/368 passing across mobile** — was 367).
- **2026-09-02 (session 43):** **Resolved the four minor deviations from the P0 review** (user-directed):
  1. **Auto-naming (§8.1):** `generateDefaultWorkoutName` now picks ONE of the two spec names per band (Random-injectable for deterministic tests); the old behavior concatenated both names (and the midnight band used a non-spec name). Test updated to assert band membership + seeded determinism.
  2. **PR vault filter/sort (§10.2):** the vault gained a control row — an exercise dropdown (`ALL EXERCISES` + per-exercise, hidden when only one exists) and a `NEWEST ↔ BEST VALUE` sort toggle. Date mode keeps the stream's newest-first order; value mode ranks rows and groups by record value. Covered by a widget test that seeds date/value-disagreeing records and asserts both orderings plus the filter narrowing.
  3. **In-session quick-info sheet (§8.1 block header):** tapping an exercise block's header opens a Glassmorphism sheet — muscles (primary/also, from the exercise catalog) and the last completed performance ("Last: N sets · best W kg × R", or the designed no-history baseline) — read offline via `ExerciseRepository.getExerciseById` + `WorkoutRepository.getLastCompletedSetsForExercise`. Widget test seeds a completed 80×8 workout, taps the header, and asserts the sheet.
  4. **Localization-ready (§15):** gen-l10n infrastructure is live — `l10n.yaml`, `flutter_localizations`, `generate: true`, `lib/l10n/app_en.arb` (~110 keys), committed `app_localizations*.dart` outputs, delegates installed in `AvenFitApp`, and a null-safe `l10nOf(context)` helper (`core/l10n/l10n.dart`) that falls back to the English template so bare widget-test harnesses need no delegate wiring (tests keep asserting English). UI literals externalized across the main shell, Home, Profile, Progress, PR vault, and the workout-core screens/widgets (active workout, block card, set row labels, rest bar, dialogs, quick-info sheet, error widget). Remaining screens (exercise/routine/nutrition/auth/history/detail lists) follow the identical mechanical pattern and are listed for the follow-up pass; domain-derived display strings (relative dates in history, volume formats, record-type labels) stay at the domain boundary until the V1.1 localization pass handles plurals properly.
  - Gates: `flutter gen-l10n` clean, `flutter analyze` (0 issues), `flutter test` (**369/369 passing across mobile** — was 368: +quick-info test, +vault filter/sort test, auto-naming test reworked).
- **2026-09-03 (session 44):** **Deep forensic audit across P0 codebase & creation of `ISSUE_FIX.md`**:
  - Performed comprehensive review of P0 vertical slices against `FEATURES.md`, `ARCHITECTURE.md`, and `AGENTS.md`.
  - Discovered 11 actionable items: 2 critical database bugs (UNIQUE constraint crash on set insertion after set deletion, PR corruption/loss on set un-completion/delete), 1 lifecycle leak (orphaned foreground service/timer on session conflict override), 1 UX blocker (broken 60s single-tap path from Home), 4 spec/data deviations (clarified ghost prefill & routine scoping [D1], unconfirmed swipe-to-delete [L7], warmup pyramid insertion order, unlinked guest data on login), 1 performance optimization (full table in-memory aggregates in lifetime/glance stats), 1 schema migration gap, and 1 backend sync contract discrepancy.
  - User provided guidance on **D1**: Ghost prefill should show historic Set N values falling back to the previous set (Set N-1) in the exercise if no history exists; routine values are strictly scoped to workouts started from the Routine section.
  - Created [`ISSUE_FIX.md`](ISSUE_FIX.md) at repo root to act as the persistent issue tracker and remediation plan across future sessions. All 11 issues cataloged and set to `[ ] OPEN`.
  - Gates: Analyzer clean, 370/370 mobile tests passing, backend gradle test clean.
- **2026-09-03 (session 45):** **Executed Batch 1 Remediation (ISSUE-01 through ISSUE-05)**:
  1. **ISSUE-01 (`UNIQUE` constraint crash on set insertion after set deletion):** In `ActiveWorkoutController.addSet`, replaced length-based `nextSetNumber` with `setsForExercise.map((s) => s.setNumber).fold(0, max) + 1`. In `deleteSet`, renumber surviving sets monotonically `1..N`. Fixed `WorkoutDao.updateSetRecord` and `WorkoutRepository.updateSet` to accept and write `setNumber` to SQLite. Verified with a regression test in `active_workout_screen_test.dart`.
  2. **ISSUE-02 (Broken 60s first workout path from Home):** Added `HomeController.startNewWorkout()` which creates an active workout session in SQLite and navigates immediately from `_StartSessionButton._start`, fulfilling FEATURES.md §5.1 without the redundant second tap on "NO ACTIVE WORKOUT".
  3. **ISSUE-03 (PR vault corruption / lost records on set un-complete or delete):** Triggered self-healing `prRepository.recomputePRs(targetSet.exerciseId!)` on set un-complete (`toggleSetCompleted`), set deletion (`deleteSet`), and completed set edits (`updateSet`). Verified with PR replay in `active_workout_screen_test.dart`.
  4. **ISSUE-04 (Orphaned foreground service & rest timer on conflict override):** Wired `ref.read(workoutForegroundServiceProvider).stop()` and `restTimerController.cancel()` into `resolveOneSessionRule` for both `saveAsCompleted` and `discard` branches. Added assertions verifying `foreground.stopCalls == 1` in `session_restore_test.dart`.
  5. **ISSUE-05 / D1 (Clarified Ghost Prefill & Routine Scoping):** Refactored `GhostPrefillService.resolveGhostSet` to scope routine targets exclusively to routine-started workouts, prioritize historical Set N for ad-hoc workouts, and fall back to the last set done in the exercise in the active session. Fixed fallback weights in `exercise_block_card.dart`. Verified via 9 unit tests in `ghost_prefill_test.dart`.
  - Gates: `flutter analyze` clean (0 issues), `flutter test` (**372/372 passing across mobile** — was 370). Marked ISSUE-01 to ISSUE-05 as `[x] FIXED` in `ISSUE_FIX.md`.
- **2026-09-03 (session 46):** **Executed Batch 2 Remediation (ISSUE-06 through ISSUE-10)**:
  1. **ISSUE-06 (Unconfirmed swipe-to-delete on completed sets, Law L7):** Added `confirmDismiss` callback to `Dismissible` in `SetRowWidget`. Uncompleted sets dismiss immediately without confirmation (§8.1 zero-friction row action); completed sets prompt with a glassmorphic confirmation dialog (`"DELETE COMPLETED SET?"` with CANCEL/DELETE). Added widget test in `active_workout_screen_test.dart`.
  2. **ISSUE-07 (Warm-up pyramid insertion order, FEATURES.md §8.1):** Updated `ActiveWorkoutController.generateWarmupPyramid` to shift existing working sets down in `setNumber` (iterating descending to prevent SQLite collisions) and insert warm-up ladder sets at positions `1..K`, strictly preceding working sets as required by spec §8.1. Updated assertions in `active_workout_screen_test.dart`.
  3. **ISSUE-08 (Unlinked local SQLite data on user sign-in, FEATURES.md §5.4, §6.2):** Added `linkGuestDataToUser(String userId)` in `WorkoutDao` and `RoutineDao`. Injected `AppDatabase? db` into `AuthRepositoryImpl` and wired silent write-through linking in `_applySession` upon successful OTP/Google sign-in. Added test in `auth_test.dart` verifying unlinked guest workouts and routines have `userId` populated.
  4. **ISSUE-09 (Full-table scan optimizations in Lifetime & Glance stats, Law L8):** Replaced in-memory Dart loops and full-table `WorkoutSetRow` loading in `HistoryDao._computeLifetimeStats()` and `_computeWeeklyGlance()` with native SQLite aggregate queries (`SUM`, `COUNT`, `MIN`, `COALESCE`) executed in $<1\text{ms}$ with $O(1)$ memory. Verified via `home_dashboard_test.dart` & `history_test.dart`.
  5. **ISSUE-10 (Incremental migration upgrade ladder in `AppDatabase.onUpgrade`):** Added missing column migrations for `workout_sets` (`sessionExerciseId`, `isPr`, `completedAt`, `notes`) and `workout_sessions` (`routineId`, `durationSeconds`) inside `onUpgrade` (`from < 4`). Created `test/core/database/migration_test.dart` verifying schema version 8 and migration callbacks.
  - Gates: `flutter analyze` clean (0 issues), `flutter test` (**376/376 passing across mobile** — was 372). Marked ISSUE-06 through ISSUE-10 as `[x] FIXED` in `ISSUE_FIX.md`.

---

## Sources of Truth

- **Product Features & Build Order:** `FEATURES.md` (repo root — all phases P0/P1/V1.1/V2, per-screen specs)
- **Architecture Blueprint & Conventions:** `ARCHITECTURE.md` (repo root — layer anatomy, contracts, diagrams)
- **Architecture, Rules & Stack Laws (L1–L10):** `AGENTS.md`
- **Developer Setup & Run:** `README.md`
- **Sprint / Session Memory:** `HANDOFF.md` (this file)

---

## Development Strategy & Milestones (Vertical Slices)

| Slice | Focus | Status | Scope | Test Verification Targets |
|:---|:---|:---:|:---|:---|
| **Slice 1** | Scaffolding & Foundation | ✅ Completed | Flutter setup in `mobile/`, Glassmorphism theme, Drift SQLite schema, GoRouter shell, Feature-First trees, Native bindings (Sentry/WorkManager/Patrol), Root `ARCHITECTURE.md`, Canonical Reference Slice (`features/workout`) | Flutter analyze (0 issues) + Flutter test (12/12 passing) + Gradle androidTest compile |
| **Slice 2** | Exercises & Routines | ✅ Completed | Local SQLite 55-exercise library + Custom Exercises + Routines CRUD + Routine Builder + Live Startup (<1s) + PostgreSQL V3/V4 migrations + `/api/v1/exercises` & `/routines` | 53/53 Flutter tests passing (query benchmarks <100ms, cascade delete, reordering, L7 immutability) + Gradle backend tests pass |
| **Slice 3** | Active Workout Engine | ✅ Completed | Live session UI, ghost prefill, rest timer math, SQLite write-through + `/api/v1/workouts` | E2E live-session test with SQLite write-through & lock-screen service test |
| **Slice 4** | Indian Nutrition Engine | ✅ Completed | Local 948-food bundled catalog search, household units + `/api/v1/nutrition` | 275/275 Flutter tests passing (food search <300ms, katori macro math, J3 journey suite, fiber unknown-semantics) |
| **Slice 5** | Auth & Cloud Sync | ✅ Completed | Phone OTP/Google login, Profile with lifetime stats, Spring Security + `/api/v1/sync` queue (backend pre-complete) | Spring Security OTP integration test + sync push/pull integration tests (`./gradlew test`) + 340/340 Flutter tests (auth union/lifecycle, screens, profile stats & sign-out) |
| **Post-MVP** | P1/V1.1/V2 slices | ⬜ Queued | CSV importer, EMA TDEE engine, Vrat Mode, sync hardening, AI surfaces | CSV importer integrity test, EMA TDEE math test, Trainer QR payload test |

---

## Branching & Workflow

- **Active Development Branch:** `dev` (Flutter codebase & active Spring Boot backend)
- **Archive Branch:** `archive/react-native` (legacy React Native code preserved for reference)
- **Stable Branch:** `main` | **Feature Branches:** `feature/*` (branched from `dev`, merged back to `dev`)
- **Rules:** Never use destructive git commands; follow conventional commits.
